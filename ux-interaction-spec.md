AI 简历平台图片原型 UX 交互说明文档

文档版本：V0.1
原型数量：28 张
适用阶段：图片原型评审、Figma 交互连线、可用性测试、前端开发交接
适用端：桌面端 Web
最近更新：2026-09-16

1. 文档目的

本文档用于说明现有 28 张图片原型之间的用户交互关系，明确：

用户从哪里进入页面；

用户可以点击哪些区域；

点击后进入哪个页面或状态；

系统在加载、成功和失败时如何反馈；

哪些内容使用整页跳转，哪些内容使用弹窗；

Figma 应采用何种 Prototype Action；

AI、保存、支付等异步任务如何演示；

每条流程的退出方式和恢复方式；

原型完成后的验收标准。

本文档不定义最终技术实现，也不代表所有状态都必须成为独立前端路由。图片原型中的部分画面只是同一页面的不同状态。

相关文档：

product-design.md：产品定位、功能范围、页面设计和产品规则；

design.md：颜色、字体、间距、圆角和组件视觉规范；

technical-selection.md：前后端、AI、PDF、数据和部署技术方案。

2. 交互目标

本次原型需要完成四条可演示的核心任务：

新用户登录后创建第一份简历；

用户编辑简历、管理模块并控制工作区；

用户使用 AI、JD 匹配和简历诊断优化内容；

用户分享、导出，并在额度不足时完成会员购买。

2.1 用户体验原则

用户始终知道当前位置：页面标题、选中图标和面板标题保持一致。

修改可预期：AI、模块隐藏和排序都不能静默覆盖或删除内容。

过程有反馈：保存、AI、诊断和支付必须区分进行中、成功与失败。

失败可恢复：错误状态必须保留用户输入并提供重试或退出入口。

分支可返回：原型中的每条路径都不能成为无法退出的死路。

弹窗不冒充页面：分享、导出和额度不足使用 Overlay，不使用整页跳转。

图片只是视觉载体：实际点击区域由 Figma 透明 Hotspot 承载。

3. 28 张原型的角色划分

编号

原型名称

类型

是否作为 Flow 起点

说明

01

用户登录

正式页面

是

推荐作为正式登录方案

02

用户登录-无预览

备用方案

否

与 01 二选一，建议放入 Archive

03

工作台空状态

页面状态

是

新用户首次登录后的工作台

04

工作台正常状态

正式页面

是

已有简历用户的默认工作台

05

创建简历

正式页面

否

选择创建方式并填写初始信息

06

模板选择

正式页面

否

浏览并选择基础模板

07

编辑器默认状态

核心页面

是

编辑、预览和优化的主工作区

08

左侧面板收起

页面状态

否

收起内容编辑面板

09

右侧面板收起

页面状态

否

收起详细设置面板

10

左右面板同时收起

页面状态

否

专注预览模式

11

模块管理状态

页面状态

否

显示、隐藏和新增模块

12

模块排序状态

页面状态

否

调整模块顺序

13

自动保存失败

页面状态

否

保存失败但保留用户输入

14

自动保存重试中

页面状态

否

保存恢复过程

15

AI 优化生成中

页面状态

否

AI 正在处理当前内容

16

AI 修改对比

页面状态

否

用户审核并决定是否应用建议

17

AI 额度不足

弹窗

是

额度不足的商业化入口

18

JD 匹配输入

页面状态

是

粘贴目标岗位信息

19

JD 匹配结果

页面状态

否

展示证据、差距和建议

20

简历未诊断

正式页面/状态

是

诊断流程入口

21

简历诊断中

页面状态

否

诊断异步过程

22

简历诊断结果

正式页面/状态

否

问题列表和修复入口

23

分享弹窗

弹窗

否

创建、复制和关闭分享链接

24

导出 PDF 弹窗

弹窗

否

PDF 检查、生成和下载

25

会员方案

正式页面

否

套餐与权益选择

26

支付处理中

页面状态

否

等待支付平台结果

27

支付成功

页面状态

否

权益已生效

28

支付未完成

页面状态

否

支付失败、取消或未完成

3.1 备用方案处理

用户登录 与 用户登录-无预览 不应同时作为正式流程入口。评审后选择其中一张作为正式版本，另一张移动到：

99 Archive / Auth Alternatives

默认建议采用界面更克制、登录任务更突出的方案。如果右侧简历预览会分散注意力，则采用“用户登录-无预览”。

4. Figma 文件组织

建议在 Figma Design 中建立以下页面：

00 Cover
01 Foundations
02 Components
03 Prototype - Main Flow
04 Prototype - Editor States
05 Prototype - AI JD Diagnosis
06 Prototype - Export Billing
99 Archive

4.1 Frame 命名

使用“模块 / 场景 / 状态”命名：

Auth / Login / Default
Dashboard / Resume List / Empty
Dashboard / Resume List / Default
Resume Create / Method / Default
Template / Select / Default
Editor / Content / Default
Editor / Left Panel / Collapsed
Editor / Right Panel / Collapsed
Editor / Focus / Both Collapsed
Editor / Modules / Manage
Editor / Modules / Reorder
Editor / Save / Failed
Editor / Save / Retrying
Editor / AI / Generating
Editor / AI / Compare
Editor / JD / Input
Editor / JD / Result
Diagnosis / Start / Default
Diagnosis / Analysis / Loading
Diagnosis / Result / Default
Overlay / Share / Default
Overlay / Export PDF / Default
Overlay / AI Quota / Empty
Membership / Plans / Default
Payment / Result / Processing
Payment / Result / Success
Payment / Result / Incomplete

4.2 图片原型的 Hotspot 结构

Kimi 生成的图片属于扁平图，图片内按钮不能直接选中。每个 Frame 应使用以下结构：

Frame
├── Prototype Image（锁定）
└── Hotspots
    ├── Hotspot / Primary Action
    ├── Hotspot / Secondary Action
    ├── Hotspot / Back
    └── Hotspot / Close

Hotspot 使用透明矩形：

Fill 透明度：0%；

Stroke：无；

尺寸：覆盖按钮完整点击区域；

图层位于图片上方；

图层名称必须表达动作，不能使用 Rectangle 123。

5. Prototype Action 使用规则

UX 场景

Figma Action

推荐动画

时长

完整页面切换

Navigate to

Dissolve

150–200ms

返回上一页面

Back

Instant/Dissolve

150ms

打开分享、导出、额度弹窗

Open overlay

Dissolve

150ms

关闭弹窗

Close overlay

Dissolve

120–150ms

页面状态切换

Navigate to

Dissolve

150ms

重建为真实图层后的侧栏收起

Smart Animate

Ease out

200–250ms

模拟异步任务完成

After delay

Dissolve

800–1500ms

页面内模块定位

Scroll to

Smooth

200–300ms

组件 Hover/Pressed

Change to

Smart Animate

100–150ms

5.1 动画限制

当前使用整张图片时，不使用 Smart Animate 模拟真实布局变化；

After delay 只用于原型演示，不代表真实业务固定耗时；

页面跳转不使用夸张的左右飞入；

弹窗不得通过 Navigate to 伪装成独立页面；

同一按钮不能同时连接两个结果。

6. Flow 1：首次创建简历

6.1 流程图

flowchart TD
    A["用户登录"] --> B{"用户是否已有简历"}
    B -->|没有| C["工作台空状态"]
    B -->|已有| D["工作台正常状态"]
    C --> E["创建简历"]
    D --> E
    E -->|空白创建| G["编辑器默认状态"]
    E -->|模板创建| F["模板选择"]
    F --> G

6.2 用户登录

进入方式

访问产品但未登录；

私有页面会话失效；

用户主动退出后返回。

主要操作

用户操作

系统响应

Figma 连接

输入账号与密码

字段进入 Filled 状态

图片原型可不模拟真实输入

点击登录

校验输入并进入工作台

Navigate to 工作台正常或空状态

点击注册

切换至注册状态

可用同 Frame 标签，不在当前 28 张强制实现

点击忘记密码

进入找回流程

当前原型可标注“后续补充”

校验规则

账号为空：在账号输入框下显示错误；

密码为空：在密码输入框下显示错误；

登录失败：保留账号，不清空密码前的其他输入；

登录成功：根据是否存在简历进入不同工作台状态。

Figma 设置

Trigger: On click
Action: Navigate to
Destination: Dashboard / Resume List / Empty 或 Default
Animation: Dissolve, 180ms

6.3 工作台空状态

页面目标

引导首次用户理解产品流程并创建第一份简历。

主要操作

点击位置

目标

说明

创建第一份简历

创建简历

主路径

查看模板

模板选择

允许先浏览后创建

用户头像

账户菜单

当前图片未包含时可不连

限制

搜索功能在没有简历时不必成为主要交互；

空状态不能只展示“暂无数据”；

必须保留清晰的创建入口。

6.4 工作台正常状态

主要操作

点击位置

目标

Action

新建简历

创建简历

Navigate to

简历主体/编辑

编辑器默认状态

Navigate to

模板入口

模板选择

Navigate to

会员入口

会员方案

Navigate to

导出

导出 PDF 弹窗

Open overlay

简历卡片上的复制、重命名和删除可以保留为 Hotspot，但当前没有对应状态图时不强制连接。可在交互标注中注明：

复制：创建副本并刷新列表；

重命名：使用小型 Inline Edit 或 Dialog；

删除：必须二次确认；

删除失败：保留原卡片。

6.5 创建简历

用户选择

创建方式

交互结果

空白创建

生成默认模块并进入编辑器

模板创建

进入模板选择

导入旧简历

未实现时显示“即将支持”，不能进入假流程

初始信息

简历名称；

用户阶段；

求职方向；

内容语言；

初始模板。

离开处理

点击返回：回到原工作台状态；

未提交前离开：无需生成空简历；

创建失败：保留输入并允许重试。

6.6 模板选择

主要操作

点击位置

系统响应

模板缩略图

更新选中状态和右侧预览

使用该模板

创建简历并进入编辑器

返回

回到创建简历

会员模板

若无权益，进入会员方案或额度说明

模板选择只改变版式，不应改变用户内容。

7. Flow 2：编辑器工作区

7.1 状态关系

flowchart TD
    A["编辑器默认状态"] --> B["左侧面板收起"]
    A --> C["右侧面板收起"]
    B --> D["左右面板同时收起"]
    C --> D
    A --> E["模块管理状态"]
    E --> F["模块排序状态"]
    A --> G["自动保存失败"]
    G --> H["自动保存重试中"]
    H --> A

7.2 编辑器默认状态

页面区域

区域

职责

顶部导航

返回、文件名、保存、撤销、重做、分享、导出

左侧图标栏

快速定位简历内容模块

左侧内容面板

结构化编辑当前模块

中间预览区

A4 实时预览

右侧设置面板

模板、布局、排版、设计、AI、JD

右侧图标栏

切换详细设置功能

底部悬浮工具

缩放和适合页面

顶部操作

点击位置

响应

返回

回到工作台正常状态

文件名

进入重命名状态

撤销

恢复上一次编辑

重做

重做被撤销内容

分享

Open overlay 分享弹窗

导出 PDF

Open overlay 导出 PDF 弹窗

左侧图标栏

Hover：显示 Tooltip；

On click：定位到相应编辑模块；

当前模块：显示选中状态；

图标下不常驻显示文字；

图片原型可直接使用 Hotspot + Scroll to，或仅模拟切换选中模块。

表单交互

输入后 A4 预览同步变化；

保存使用防抖，不在每次按键时闪动状态；

空模块不出现在 A4 预览；

字段错误在字段附近显示；

删除条目与隐藏模块是两种不同操作。

7.3 左侧面板收起

触发

点击左侧内容面板边界的收起按钮。

结果

内容编辑面板收起；

左侧图标栏保留；

A4 预览占用更多空间并重新居中；

收起按钮变为展开按钮；

点击模块图标时可自动展开对应表单。

连接关系

操作

目标

点击左侧展开

编辑器默认状态

点击右侧收起

左右面板同时收起

7.4 右侧面板收起

触发

点击右侧设置面板边界的收起按钮。

结果

详细设置面板收起；

右侧功能图标栏保留；

A4 预览重新居中；

点击右侧功能图标时重新展开对应面板。

连接关系

操作

目标

点击右侧展开

编辑器默认状态

点击左侧收起

左右面板同时收起

7.5 左右面板同时收起

页面目的

提供专注预览模式，而不是进入新的业务页面。

操作

点击位置

目标

左侧展开

右侧面板收起状态

右侧展开

左侧面板收起状态

左右全部展开

编辑器默认状态（如有此按钮）

7.6 模块管理状态

用户可以执行

显示模块；

隐藏模块；

新增自定义模块；

进入模块排序；

恢复默认模块。

UX 规则

隐藏不等于删除；

隐藏后数据仍然保留；

A4 预览不显示隐藏模块标题和空白；

必要模块不能删除；

删除自定义模块前二次确认。

连接

操作

目标

管理模块顺序

模块排序状态

完成/关闭

编辑器默认状态

7.7 模块排序状态

用户操作

拖拽模块列表项；

查看插入位置；

完成排序；

恢复默认顺序。

反馈

被拖动项轻微抬起；

插入位置清晰但不过度强调；

A4 预览同步更新；

排序不改变模块内容和显示状态。

当前图片原型可以将“完成排序”连接回编辑器默认状态，不强制模拟真实 Drag。

7.8 自动保存失败与重试

保存失败

顶部显示“保存失败”；

用户输入保留在本地界面；

A4 预览继续更新；

提供“重新保存”；

用户离开前提示存在未保存内容。

Figma 连接

自动保存失败
  On click “重新保存”
  → 自动保存重试中

自动保存重试中
  After delay 1200ms
  → 编辑器默认状态

注意

原型中的自动成功只用于演示。真实产品应根据服务端结果进入成功或再次失败。

8. Flow 3：AI 内容优化

8.1 流程图

flowchart TD
    A["编辑器默认状态"] --> B{"内容和额度检查"}
    B -->|额度充足| C["AI 优化生成中"]
    B -->|额度不足| D["AI 额度不足弹窗"]
    C --> E["AI 修改对比"]
    E -->|接受| F["应用修改并返回编辑器"]
    E -->|拒绝| G["保留原文并返回编辑器"]
    E -->|重新生成| C
    D --> H["会员方案"]

8.2 AI 优化生成中

进入条件

当前内容达到最低要求；

用户选择优化目标；

权益检查通过；

额度已预占。

界面行为

原文保持可见；

A4 仍显示原文；

展示处理步骤，不显示虚假百分比；

提供“取消生成”；

明确“生成完成前不会修改简历”。

Figma 演示

Trigger: After delay
Delay: 1200–1500ms
Action: Navigate to
Destination: Editor / AI / Compare

8.3 AI 修改对比

信息结构

优化目标；

原始内容；

建议内容；

新增、删除和替换差异；

修改原因；

事实风险；

本次额度信息。

操作与结果

操作

系统行为

目标状态

接受修改

应用建议、创建版本、更新预览

编辑器默认状态

拒绝

丢弃建议、保留原文

编辑器默认状态

重新生成

再次提示额度规则

AI 优化生成中

补充事实

返回相关表单

编辑器默认状态

信任规则

AI 结果不得自动覆盖；

缺乏事实依据的文本不能一键应用；

接受后仍能撤销；

拒绝不再次扣费；

重新生成是否扣费必须提前说明。

8.4 AI 额度不足

该原型应作为 Overlay 打开，而不是离开编辑器。

Overlay 设置

Action: Open overlay
Position: Centered
Background: Black 50%
Close when clicking outside: 可开启
Animation: Dissolve 150ms

操作

操作

目标

查看会员方案

会员方案

购买 AI 次数包

会员方案对应区域

暂时不用

Close overlay

关闭弹窗后必须保留当前编辑内容和 JD 输入。

9. Flow 4：JD 匹配

9.1 流程图

flowchart TD
    A["编辑器默认状态"] --> B["JD 匹配输入"]
    B --> C{"内容和额度检查"}
    C -->|通过| D["JD 匹配结果"]
    C -->|额度不足| E["AI 额度不足弹窗"]
    D --> F["AI 修改对比"]
    F --> G["编辑器默认状态"]

9.2 JD 匹配输入

输入项

岗位名称；

公司名称（可选）；

JD 文本；

分析范围；

额度提示。

操作

操作

结果

开始匹配

进入 JD 匹配结果（演示）

清空

清空 JD 文本，保留简历

返回

回到编辑器默认状态

校验

JD 为空：禁用“开始匹配”；

内容过短：提示补充完整职责和要求；

请求失败：保留 JD 文本；

额度不足：打开额度不足 Overlay。

9.3 JD 匹配结果

结果必须分为：

已满足：简历中存在明确证据；

可强化：存在相关经历，但表达不足；

未证明：当前简历没有事实依据。

操作

操作

结果

定位内容

返回编辑器并定位对应模块

AI 辅助修改

进入 AI 修改对比

标记不适用

当前建议进入忽略状态

重新分析

返回 JD 匹配输入或重新进入结果

“未证明”不等于“用户不具备”，不能直接生成虚构经历。

10. Flow 5：简历诊断

10.1 流程图

flowchart LR
    A["简历未诊断"] --> B["简历诊断中"]
    B --> C["简历诊断结果"]
    C --> D["编辑器默认状态"]

10.2 简历未诊断

用户看到

诊断范围；

当前简历名称；

预计额度；

AI 不会自动修改的提示；

开始诊断按钮。

连接

On click “开始诊断”
→ 简历诊断中

10.3 简历诊断中

过程步骤

检查内容完整性；

分析经历表达；

检查岗位相关性；

检查格式与 ATS 基础风险。

交互规则

已完成步骤显示对勾；

当前步骤显示加载；

不显示虚假百分比；

允许取消并返回；

诊断失败时不扣除或返还额度。

原型连接

After delay 1500ms
→ 简历诊断结果

10.4 简历诊断结果

问题结构

优先级；

问题标题；

所在模块；

当前证据；

修改建议；

定位与修复操作。

操作

操作

目标

定位内容

编辑器默认状态，对应模块

AI 辅助修改

AI 修改对比

标记不适用

结果页当前状态

重新诊断

简历诊断中

诊断分数只用于问题排序，不宣称等同于招聘系统真实评分。

11. Flow 6：分享与导出

11.1 分享弹窗

分享弹窗必须裁剪为独立 Overlay Frame，不能把带编辑器背景的整张图片直接作为 Overlay。

打开方式

编辑器顶部“分享”
→ Open overlay
→ Overlay / Share / Default

主要操作

操作

系统响应

开启分享

创建只读链接

复制链接

显示“已复制”Toast

设置有效期

更新链接有效期

允许下载

更新访问权限

关闭分享

使旧链接失效

关闭弹窗

Close overlay

UX 规则

分享默认关闭；

分享链接只读；

关闭分享后旧链接失效；

复制成功要有反馈；

分享失败不改变原有链接状态。

11.2 导出 PDF 弹窗

打开方式

编辑器顶部“导出 PDF”
→ Open overlay
→ Overlay / Export PDF / Default

信息

文件名；

文件格式；

当前页数；

分页检查；

权益状态；

生成与下载按钮。

操作

操作

系统响应

生成 PDF

进入按钮 Loading 状态

下载

生成完成后触发下载

修复分页

关闭弹窗并返回编辑器

关闭

Close overlay

失败处理

生成失败：显示原因和重试；

不重复创建同一导出任务；

额度或权益不足：进入会员方案；

PDF 与当前保存版本不一致时给出提示。

12. Flow 7：会员与支付

12.1 流程图

flowchart TD
    A["AI 额度不足"] --> B["会员方案"]
    B --> C["支付处理中"]
    C -->|支付确认| D["支付成功"]
    C -->|取消或失败| E["支付未完成"]
    D --> F["返回原任务"]
    E -->|重新支付| C
    E -->|更换套餐| B

12.2 会员方案

用户信息

当前套餐；

剩余额度；

免费版；

会员版；

AI 次数包；

权益差异；

价格和周期。

操作

操作

目标

选择套餐

更新选中状态

立即购买

支付处理中

返回

返回额度不足前的任务或工作台

价格、额度和周期未正式确定时使用 TBD，不得在原型中把示例数字当成最终商业规则。

12.3 支付处理中

界面行为

显示订单编号；

显示“正在确认支付结果”；

提醒不要重复支付；

提供刷新支付状态；

允许稍后查看。

Figma 分支演示

支付处理中不能同时使用 After delay 跳向成功和失败。应建立两个 Flow：

Flow 7A / Payment Success：处理中 → 成功；

Flow 7B / Payment Incomplete：处理中 → 未完成。

也可以在“支付处理中”放两个仅供原型测试的透明 Hotspot：

Hotspot / Simulate Success；

Hotspot / Simulate Failure。

这些测试热区不得出现在最终开发界面中。

12.4 支付成功

用户看到

支付成功；

订单信息；

会员已生效；

当前权益；

返回原任务。

操作

操作

目标

返回继续优化简历

编辑器默认状态或原任务

查看订单

个人中心订单页（当前可暂不连）

返回会员中心

会员方案

12.5 支付未完成

支付未完成可能表示失败、取消或订单尚未支付，界面不能默认宣称已扣款。

操作

操作

目标

重新支付

支付处理中

选择其他套餐

会员方案

返回原任务

编辑器默认状态，权益不变

13. 全局返回与退出规则

当前场景

返回行为

登录

返回公开首页（当前未制作时可不连接）

创建简历

返回原工作台状态

模板选择

返回创建简历

编辑器

返回工作台正常状态

AI 生成中

取消后回到编辑器，保留原文

AI 修改对比

关闭后保留原文

JD 输入

返回编辑器并保留草稿

JD 结果

返回编辑器或 JD 输入

诊断中

取消后回到未诊断或上一次结果

诊断结果

返回编辑器

分享/导出/额度弹窗

Close overlay

会员方案

返回原任务或工作台

支付成功

返回原任务

支付未完成

返回会员方案或原任务

14. Tooltip 与图标交互

14.1 左右图标栏

图标下方不显示常驻文字；

Hover 300–500ms 后显示 Tooltip；

Tooltip 靠近图标但不遮挡目标内容；

Tooltip 使用功能名称，如“基本信息”“布局”“AI 优化”；

当前选中项使用浅灰背景；

不使用大面积蓝色选中方块。

14.2 Figma 实现

图片原型阶段可以不为每个 Tooltip 单独创建画板。进入组件重建后，创建：

Icon Button
├── Default
├── Hover + Tooltip
├── Selected
└── Disabled

使用 While hovering → Change to Hover + Tooltip。

15. 键盘与可访问性交互

虽然当前是图片原型，正式重建时应满足：

Tab 可以依次访问按钮、输入框、图标入口；

Enter/Space 可以触发当前按钮；

Esc 可以关闭非强制弹窗；

图标按钮必须有可访问名称；

焦点不能只依靠颜色表达；

拖拽排序必须提供上移/下移替代方式；

AI 差异不能只使用红绿颜色区分；

错误信息必须包含文字说明；

文字和背景满足基本对比度。

16. 状态反馈标准

任务

Idle

Loading

Success

Error

自动保存

已保存

正在保存

已保存

保存失败、重试

AI 优化

可以优化

生成中

展示对比

返还额度、重试

JD 匹配

可以分析

分析中

展示结果

保留 JD、重试

简历诊断

尚未诊断

诊断中

展示问题

保留简历、重试

PDF 导出

可以生成

生成中

可以下载

显示原因、重试

分享链接

未开启

创建中

可以复制

保留原状态

支付

待支付

确认中

权益生效

未完成、可重试

17. 原型测试 Flow 设置

建议设置以下 Starting Point：

Flow 名称

起始 Frame

测试目标

Flow 1 / First Resume

用户登录

登录、创建、模板、进入编辑器

Flow 2 / Editor Workspace

编辑器默认状态

面板收起、模块管理、保存恢复

Flow 3 / AI Optimization

编辑器默认状态

AI 生成、对比、接受或拒绝

Flow 4 / JD Matching

JD 匹配输入

JD 输入、结果、定位修改

Flow 5 / Resume Diagnosis

简历未诊断

诊断中、诊断结果、去修改

Flow 6 / Export and Share

编辑器默认状态

打开、关闭分享与导出弹窗

Flow 7A / Payment Success

AI 额度不足

会员、处理中、支付成功

Flow 7B / Payment Incomplete

AI 额度不足

会员、处理中、支付未完成

18. 可用性测试任务

让测试用户在不接受讲解的情况下完成：

登录产品；

创建一份前端开发简历；

选择一个 ATS 友好的模板；

在编辑器中找到项目经历；

隐藏校园经历；

调整项目经历的位置；

收起左右面板查看 A4；

使用 AI 优化一段项目经历；

拒绝一条不真实的 AI 建议；

粘贴 JD 并查看未被证明的能力；

运行简历诊断并定位一个问题；

分享简历；

导出 PDF；

在额度不足时查看会员并模拟支付。

18.1 观察重点

用户是否能区分左侧内容与右侧设置；

用户是否能发现面板收起按钮；

用户是否理解隐藏不等于删除；

用户是否知道 AI 结果还没有应用；

用户是否能找到拒绝修改；

用户是否理解“未证明”而非“不具备”；

用户是否知道保存是否成功；

用户是否能关闭弹窗并返回原任务；

付费流程是否保留原输入。

19. Figma 连线检查清单

19.1 页面结构

所有图片都位于 Frame 内；

图片图层已锁定；

所有 Hotspot 位于图片上方；

Frame 使用统一命名；

登录备用方案已移入 Archive；

弹窗已裁剪为独立 Overlay Frame。

19.2 主流程

登录可以进入工作台；

工作台可以进入创建简历；

创建简历可以进入模板或编辑器；

模板选择可以进入编辑器；

编辑器可以返回工作台；

所有关键流程存在 Starting Point。

19.3 编辑器

左侧面板可以收起和展开；

右侧面板可以收起和展开；

两侧同时收起后仍能恢复；

模块管理可以进入模块排序；

模块排序可以返回编辑器；

保存失败可以进入重试并恢复。

19.4 AI、JD 与诊断

AI 生成能够进入修改对比；

接受与拒绝都有明确目标；

重新生成可以返回生成中；

额度不足可以关闭或进入会员；

JD 输入能够进入结果；

JD 建议能够进入 AI 对比；

诊断流程可以完整走通。

19.5 弹窗与支付

分享使用 Open overlay；

导出使用 Open overlay；

弹窗关闭使用 Close overlay；

会员可以进入支付处理中；

支付成功和未完成使用不同 Flow；

所有支付结果都存在返回路径。

19.6 体验质量

没有无法退出的死路页面；

同一个按钮没有冲突的多条连接；

页面跳转动画保持克制；

After delay 只用于演示异步完成；

图片中相同按钮的 Hotspot 尺寸一致；

Presentation 模式下完整测试全部 Flow。

20. 原型与真实产品的差异说明

以下行为在 Figma 中只是模拟：

文本输入和表单校验；

自动保存请求；

AI 生成耗时；

JD 真实解析；

诊断计算；

PDF 文件生成；

分享链接复制；

额度扣减；

支付平台回调；

会员权益实时生效。

原型中的 After delay、模拟成功 Hotspot 和固定示例内容不能直接转化为业务代码。开发阶段需要根据 product-design.md 和接口设计实现真实状态机。

21. 交付完成标准

本轮 UX 交互原型在满足以下条件后可以冻结为 V0.1：

28 张图片完成正式/状态/弹窗/备用分类；

登录方案完成二选一；

7 条核心 Flow 都可以从头走到结束；

分享、导出和额度不足使用 Overlay；

AI 修改能够接受、拒绝和重新生成；

JD 与诊断都能定位回编辑器；

保存失败存在恢复路径；

支付成功和未完成分支清楚；

每个页面都有返回或关闭方式；

3–5 名目标用户完成一次可用性测试；

测试中发现的阻断问题已经修复；

页面和组件视觉符合 design.md；

产品行为符合 product-design.md。

这 28 张图片不是 28 个互相独立的页面，而是一套由正式页面、交互状态、弹窗和异常分支组成的完整产品流程。Figma 原型的目标不是展示所有图片，而是让用户可以自然完成任务，并且在任何一步都能理解系统状态、继续操作或安全返回。