AI 简历平台产品设计文档

文档版本：V0.1
文档状态：MVP 产品基线
适用范围：Web 桌面端
最近更新：2026-09-15

1. 文档说明

本文档用于统一产品定位、用户范围、功能边界、页面结构、关键流程、交互规则和验收标准，并作为后续低保真原型、高保真 UI、数据模型、接口设计和开发拆分的产品依据。

相关文档：

design.md：界面视觉规范，定义配色、字体、间距、组件和编辑器布局；所有新页面设计前必须先阅读并遵循。

technical-selection.md：技术选型与系统实现方向。

本文档：回答产品为什么存在、为谁服务、提供什么能力、页面如何工作。

1.1 决策优先级

需求发生冲突时，按以下顺序判断：

用户真实经历与数据安全；

核心求职任务是否能够完成；

用户是否拥有最终修改决定权；

MVP 范围与实现成本；

视觉表现与动效完善。

2. 产品概述

2.1 产品名称

暂定名称：AI 简历平台。正式品牌名称待确定。

2.2 一句话定位

帮助学生、应届毕业生和职场人士，把真实但零散的经历整理成专业、可信、针对目标岗位的简历，并以稳定的版式导出。

2.3 产品不是什么

本产品不是单纯的简历模板商城，也不是一键生成虚构经历的 AI 写作工具。模板和 AI 都是手段，核心价值是帮助用户完成以下转换：

零散真实经历 → 结构化内容 → 专业表达 → 岗位匹配 → 可投递简历

2.4 产品愿景

让缺少简历写作经验的用户，也能在不虚构事实的前提下，独立完成一份内容清晰、版式专业、适合目标岗位且便于持续维护的简历。

3. 背景与问题

3.1 用户面临的主要问题

问题

典型表现

产品机会

不知道写什么

面对空白表单无从下手，遗漏有价值的经历

通过结构化字段和引导式提问帮助回忆

经历写成流水账

只描述“做过什么”，缺少行动、结果和价值

AI 基于原始事实优化表达结构

不会针对岗位调整

同一份简历投递所有岗位

分析 JD 关键词、职责和能力要求

不懂排版

字号、间距、边距随意，Word 容易错位

提供受约束的专业模板与实时 A4 预览

担心 ATS

复杂图形、列布局或关键词缺失

提供结构诊断和基础 ATS 风险提示

不信任 AI

AI 可能编造数字、职责或技能

明确证据边界，所有修改必须由用户确认

修改成本高

针对不同岗位需要反复复制和调整

支持版本、复制、局部修改和岗位版本管理

3.2 为什么值得做

市场上已有大量模板工具，但“好看”并不能自动解决内容质量和岗位匹配。产品的意义不是再提供一个画布，而是将内容引导、可信 AI、岗位分析和稳定排版连接为一条完整工作流。

4. 目标用户

4.1 第一目标用户：大学生与应届毕业生

特征：

简历经验少，不确定该写哪些内容；

项目、课程、竞赛、社团和实习经历比较零散；

缺少可量化结果，容易套用空泛话术；

经常同时投递多个方向；

对价格较敏感，但愿意为明显提高效率和质量的功能付费。

核心任务：

从零创建第一份简历；

把校园和项目经历转换成岗位相关表达；

快速适配不同 JD；

导出专业、无错位的 PDF。

4.2 第二目标用户：1–5 年工作经验的职场人士

特征：

已有旧简历，但内容陈旧或长期未整理；

更重视职业概要、工作经历和项目成果；

希望针对具体岗位快速建立简历版本；

对隐私、专业感和编辑效率要求更高。

核心任务：

整理近期工作成果；

弱化不相关的校园经历；

突出岗位能力和业务结果；

管理多个投递版本。

4.3 暂不重点服务的用户

需要高度视觉化作品集的设计师；

需要学术 CV、论文列表和引用管理的研究人员；

需要批量管理候选人的企业招聘团队；

需要完全还原任意 Word 复杂格式的用户。

5. 产品价值与差异化

5.1 核心价值

价值

用户感知

产品实现

有内容可写

不再面对空白页

分模块填写、示例、引导问题

表达更专业

经历更清晰但仍然真实

单段 AI 优化、修改原因、事实约束

更适合岗位

知道缺什么、改哪里

JD 解析、匹配评分、证据定位、建议列表

排版可靠

所见接近所得

A4 实时预览、受约束的排版参数、PDF 导出

修改可控

AI 不会擅自覆盖

前后对比、逐项接受或拒绝、版本记录

可持续维护

不必每次从头制作

多简历、复制版本、自动保存、历史版本

5.2 差异化原则

内容优先于模板数量；

可信修改优先于“一键生成”；

岗位针对性优先于通用润色；

受约束的专业排版优先于无限自由画布；

清晰、克制的工具体验优先于营销式 AI 装饰。

6. 产品目标、非目标与成功标准

6.1 MVP 产品目标

用户能够在一个连续流程中完成创建、填写、优化、预览和导出。

新手能够理解每个模块该填写什么。

AI 修改不破坏原文，且不会凭空创造事实。

用户能够根据目标 JD 获得可执行的修改建议。

编辑预览与最终 PDF 在内容和版式上尽量一致。

付费权益、额度消耗和失败返还规则清晰可解释。

6.2 非目标

MVP 不追求：

海量模板；

像设计软件一样自由摆放元素；

自动代替用户完成全部经历；

保证获得面试或通过所有 ATS；

自动投递职位；

多人实时协作；

移动端完整编辑体验；

论文排版功能。

6.3 MVP 成功标准

首轮验证重点不是注册量，而是核心任务是否真正完成：

指标

定义

初期目标

创建完成率

创建后至少完成基本信息、教育/工作和一个经历模块

上线测试后设基线

首次导出率

新用户在首次创建后成功导出 PDF

上线测试后设基线

AI 建议接受率

用户接受的建议数 / 展示的建议数

用于判断建议质量

JD 流程完成率

粘贴 JD 后查看并处理建议的用户比例

用于判断岗位价值

保存可靠性

保存成功且无内容丢失的编辑会话比例

接近 100%

PDF 一致性

预览与导出无明显内容、分页和字体差异

阻断性问题为 0

付费转化率

触达权益限制后完成购买的比例

套餐确定后设目标

所有数值型目标需在可用性测试和内测后确定，不在当前文档中虚构。

7. 产品体验原则

7.1 真实优先

AI 只能改写用户已提供的信息；

未提供的数字、技能、公司、奖项、职责和结果不得自动补充；

缺少关键信息时先提出问题或使用明确占位提示；

推测内容必须标注为“待用户确认”，不能直接写入简历。

7.2 用户掌控

自动保存可以保存用户输入，但 AI 结果不能自动覆盖原文；

每次 AI 修改都提供原文、建议文本和修改原因；

用户可以接受、拒绝、逐项应用、撤销和恢复版本；

删除简历、覆盖版本、付费和消耗额度等操作必须有明确反馈。

7.3 渐进式复杂度

默认界面只展示当前任务所需的控制；

高级排版参数收纳在右侧设置面板；

新用户看到示例和引导，熟练用户可以快速跳转和批量操作；

不强迫所有用户填写校园、实习或奖项模块。

7.4 内容与版式分离

左侧负责事实和文字内容；

中间负责最终文档预览；

右侧负责布局、排版、设计、页面、AI 和 JD；

修改模板不应丢失内容；

隐藏模块只影响显示，不删除原始数据。

7.5 专业工具感

视觉与组件严格遵循 design.md。界面保持深色、紧凑、低装饰，A4 白纸是视觉中心；不使用大面积品牌色、渐变光效、大圆角卡片或无实际作用的 AI 装饰。

8. MVP 范围

8.1 必须开发

模块

MVP 能力

用户认证

注册、登录、退出、会话保持

简历管理

创建、重命名、复制、删除、最近编辑

结构化填写

按模块填写，新增条目，校验必要字段

实时预览

A4 预览、分页反馈、缩放

模块管理

显示、隐藏、新增、拖拽排序

排版设计

模板、字体、字号、字重、行高、颜色、边距

自动保存

保存状态、失败重试、离开保护

AI 单段优化

选择优化目标，返回结构化修改建议

AI 修改对比

原文/建议/原因，接受、拒绝、重新生成

简历诊断

完整性、清晰度、量化、结构和基础 ATS 风险

JD 匹配

解析岗位、匹配证据、差距与修改建议

模板切换

保留内容并切换受支持模板

导出分享

PDF 生成、下载；受控链接分享

权益与额度

套餐展示、权益检查、额度流水

支付基础能力

创建订单、支付结果、回调幂等、权益生效

8.2 可在 MVP 后半程加入

旧简历 PDF/Word 导入与结构化解析；

简历历史版本浏览和命名；

多语言简历内容辅助；

更多 ATS 规则；

公开分享页的访问统计；

模板收藏与筛选。

8.3 暂不开发

App 和小程序；

自动投递；

模拟面试；

简历社区；

多人协作；

大量模板市场；

论文编辑器；

任意 Word 格式无损还原；

自动续费（如商业决策未确认）；

微服务拆分。

9. 信息架构

flowchart TD
    A["公开区域"] --> A1["产品首页"]
    A --> A2["登录与注册"]
    A --> A3["套餐说明"]
    B["用户区域"] --> B1["简历工作台"]
    B --> B2["创建简历"]
    B --> B3["简历编辑器"]
    B --> B4["导出与分享"]
    C["账户区域"] --> C1["会员与额度"]
    C --> C2["订单记录"]
    C --> C3["个人设置"]
    D["运营区域"] --> D1["管理后台（后期）"]

9.1 推荐导航关系

登录后默认进入“简历工作台”；

创建完成后直接进入“简历编辑器”；

AI 对比、JD 匹配、布局与排版优先作为编辑器内面板，而不是频繁跳转独立页面；

导出可由编辑器顶部触发，生成过程在弹窗或任务面板中反馈；

会员中心可从工作台、账户菜单和额度不足提示进入。

10. 核心用户流程

10.1 首次创建与导出

flowchart TD
    A["注册或登录"] --> B["进入工作台"]
    B --> C["新建简历"]
    C --> D["选择空白或模板"]
    D --> E["填写结构化内容"]
    E --> F["实时预览与自动保存"]
    F --> G["AI 优化或诊断"]
    G --> H["确认修改"]
    H --> I["调整模板与排版"]
    I --> J["导出 PDF"]

10.2 AI 单段优化

flowchart TD
    A["选择内容与优化目标"] --> B{"内容与额度有效？"}
    B -->|否| C["提示补充内容或获取权益"]
    B -->|是| D["预占额度并调用 AI"]
    D --> E{"生成成功？"}
    E -->|否| F["返还额度并允许重试"]
    E -->|是| G["展示前后对比与原因"]
    G --> H{"用户决定"}
    H -->|接受| I["应用修改并创建版本"]
    H -->|拒绝| J["保留原文"]

10.3 JD 匹配

flowchart TD
    A["粘贴目标 JD"] --> B["识别岗位职责与关键词"]
    B --> C["匹配简历中的证据"]
    C --> D["展示优势、缺口和风险"]
    D --> E["生成逐条修改建议"]
    E --> F["用户确认后应用"]

JD 匹配必须区分三类结果：

已满足：简历中存在明确证据；

可强化：已有相关经历，但表达不足；

未证明：JD 有要求，但简历中没有事实依据。此类结果只能提示补充或确认，不能由 AI 编造。

10.4 自动保存

stateDiagram-v2
    [*] --> 已保存
    已保存 --> 有未保存修改: 用户输入
    有未保存修改 --> 正在保存: 防抖触发
    正在保存 --> 已保存: 成功
    正在保存 --> 保存失败: 网络或服务异常
    保存失败 --> 正在保存: 自动或手动重试

10.5 导出与分享

用户点击“导出 PDF”或“分享”；

系统检查简历内容、分页和用户权益；

不满足条件时明确显示问题及处理入口；

满足条件后创建导出任务或分享链接；

导出过程中允许用户继续停留，但不能重复创建同一任务；

成功后提供下载；失败时说明原因并允许重试；

分享链接支持关闭、重新生成和访问权限控制。

11. 功能模块设计

11.1 认证与账户

邮箱或手机号注册/登录方式根据运营地区确定；

支持退出和会话过期处理；

未登录用户访问私有简历时进入登录流程；

用户只能访问自己的简历、订单、导出文件和 AI 记录。

11.2 简历管理

新建空白简历；

从基础模板创建；

简历重命名；

复制为新版本；

删除前二次确认；

展示最近编辑时间、完成度和目标岗位；

保留空状态、加载状态和错误状态。

11.3 内容编辑

结构化字段输入；

同一模块支持多个条目，如多段教育或工作经历；

条目支持展开、折叠、复制和删除；

模块支持显示、隐藏、排序和自定义新增；

输入变化实时同步至预览；

字段错误在输入附近反馈，不集中到页面顶部。

11.4 预览与排版

A4 纸张始终作为中间视觉焦点；

实时反映内容、模块显隐、顺序和排版参数；

支持缩放、适合宽度、适合页面；

内容溢出时显示分页而非裁切；

模板切换不得删除内容；

不支持的排版组合应被约束，而非让用户产生不可导出的状态。

11.5 AI 优化

支持“更专业”“更简洁”“强调结果”“适配岗位”等明确目标；

一次优先处理一个条目或一段内容；

返回结构化建议和原因；

支持重新生成，但需显示可能再次消耗额度；

所有结果记录模型请求状态、额度流水和用户决策。

11.6 诊断与 JD 匹配

简历诊断覆盖完整性、可读性、行动表达、结果证据、重复表达、格式和基础 ATS 风险；

JD 匹配展示整体结论，同时必须提供可追溯的具体证据；

分数只用于帮助排序问题，不宣称等同于招聘系统真实评分；

建议可以定位到具体简历模块并进入修改流程。

11.7 导出与文件

首期主格式为 PDF；

文件生成过程异步时需展示进度或状态；

中文字体必须嵌入或使用稳定替代策略；

下载链接使用临时授权地址；

导出文件应包含创建时间、简历版本和模板版本信息，便于排查问题。

12. 页面清单与优先级

优先级

页面/状态

MVP

主要作用

P0

简历编辑器

是

内容填写、实时预览、排版、AI、JD、导出

P0

简历工作台

是

创建和管理多份简历

P0

创建简历

是

选择创建方式并建立初始数据

P0

AI 修改对比

是

用户审核并决定是否应用 AI 建议

P0

导出状态/弹窗

是

权益检查、生成、下载和错误处理

P1

简历诊断

是

汇总问题并引导用户逐项修复

P1

JD 匹配面板

是

分析岗位匹配并生成可信建议

P1

会员方案

是

展示套餐、权益和购买入口

P1

登录注册

是

用户认证

P1

支付结果

是

确认订单与权益状态

P2

产品首页

是

说明价值并引导创建

P2

个人中心

是

账户、额度、订单和安全设置

P2

模板中心

可合并

浏览模板；初期可并入创建页/编辑器

P3

管理后台

后期

用户、订单、模板和运营管理

13. 详细页面设计

13.1 简历工作台

页面目标

让用户快速找到最近编辑的简历，创建新简历，并完成基础管理操作。

页面区域

顶部栏：产品标识、搜索、额度入口、账户菜单；

主导航：我的简历、模板、会员与账户；

主内容：新建简历入口、简历列表或网格；

简历项：名称、目标岗位、最近编辑时间、状态、缩略图；

更多菜单：重命名、复制、删除、导出。

关键交互

点击简历主体进入编辑器；

新建按钮进入创建流程；

删除必须二次确认，并说明是否可以恢复；

搜索结果为空与用户没有任何简历使用不同文案；

卡片不堆叠无意义指标，完成度只在能给出下一步建议时显示。

必备状态

首次空状态；

加载骨架；

正常列表；

搜索无结果；

加载失败；

删除中和删除失败。

13.2 创建简历

页面目标

降低开始成本，让用户根据自身情况选择最合适的入口。

创建方式

方式

MVP 状态

行为

空白创建

可用

创建默认模块并进入编辑器

模板创建

可用

选择模板，但内容结构保持独立

导入旧简历

可标注后续支持

未实现时不能伪装为可用按钮

初始信息

简历名称；

求职方向；

用户阶段：在校/应届、在职、待业；

内容语言；

初始模板。

用户阶段只影响默认模块组合，不限制后续添加或隐藏模块。

13.3 简历编辑器

页面目标

让内容编辑、文档预览和细节优化在同一屏内完成，尽量减少页面跳转。

整体结构

界面采用沉浸式全屏工作区，并严格遵循 design.md 中的尺寸与视觉规则：

区域

主要职责

顶部导航栏

返回、文件名、保存状态、撤销/重做、分享、导出

左侧图标栏

快速跳转到内容模块，不显示常驻文字

左侧编辑面板

当前模块的结构化填写表单

中间预览区

居中显示 A4 实时预览

右侧设置面板

当前选中功能的详细设置

右侧图标栏

布局、分享、排版、设计、页面、AI、JD 快捷入口

底部悬浮工具

缩放、适合页面/宽度等预览控制

顶部导航栏

只保留高频和全局操作：

返回工作台；

简历名称与重命名；

自动保存状态；

撤销和重做；

分享；

导出 PDF。

模板、字体、颜色、页边距和缩放不放入顶部，避免工具栏拥挤。左右操作分组清晰，保存状态位于视觉中部或文件名附近。

左侧图标栏

模块包括：

基本信息；

求职意向；

职业概要；

教育背景；

工作经历；

项目经历；

实习经历；

校园经历；

技能与证书；

奖项荣誉；

自我评价。

交互规则：

图标在可用高度内均匀、分组分布，但不为了铺满而拉开到难以扫描；

图标下方不显示文字，悬浮时显示 Tooltip；

点击图标滚动并定位到对应编辑模块；

当前模块使用浅灰选中背景，不使用大面积蓝色方块；

图标栏保留独立的左面板收起/展开控制；

隐藏模块仍可在模块管理中恢复，但可降低其图标强调度。

左侧编辑面板

按模块显示结构化字段；

当前定位模块展开，其余模块可折叠；

每个经历模块支持多条记录；

表单变化实时反映到预览；

模块为空或被隐藏时，不在简历预览中显示标题和占位空间；

底部固定“新增模块”和“模块排序”入口；

删除条目与删除整个模块必须区分。

中间 A4 预览

页面始终居中并保持纯白；

周围为深色工作区，以强化内容焦点；

支持多页垂直排列；

选中表单字段时，可在预览中弱提示对应内容，但不改变导出样式；

预览中的内容默认不可直接自由拖拽，避免内容模型和排版失控；

底部悬浮缩放工具不占用顶部空间。

右侧图标栏与设置面板

功能包括：

模板；

布局；

分享；

排版；

设计；

页面；

AI 优化；

JD 匹配；

导出状态（可与顶部导出共用）。

交互规则：

图标在侧边栏对应区域内均匀、分组排列；

不在图标下显示文字，统一通过 Tooltip 说明；

点击后打开右侧对应设置；

已打开功能再次点击时可保持面板或收起，行为需全局一致；

右面板有独立收起按钮，收起后保留图标栏；

左右面板可以独立收起，扩大中间预览空间；

收起按钮采用紧贴面板边界的小型控制，不使用大浮动按钮。

布局设置

拖拽调整模块顺序；

切换模块显示/隐藏；

支持恢复默认顺序；

移动时在预览中即时反映；

空模块默认不显示，即使开关为显示，也需避免输出空标题。

排版设置

字体类型；

模块级字号和字重；

正文行高；

模块间距；

段落间距；

允许恢复模板默认值。

参数必须设定合理范围，避免极端值导致 PDF 不可用。

设计设置

简历强调色；

模块标题色；

分隔线显示及颜色；

头像显示与基础样式；

不提供与专业简历无关的渐变、光效和复杂装饰。

页面设置

内容语言；

纸张格式，MVP 至少支持 A4；

上、下、左、右页边距；

页码或分页控制（如模板支持）；

边距可以联动或分别调整。

响应与性能体验

文本输入后预览反馈应接近即时；

保存采用防抖，不应每个按键都产生明显网络状态跳动；

AI 和 PDF 属于较慢任务，必须有进行中状态；

调整排版时不阻断输入；

页面离开前若仍有未保存修改，应给予明确提示。

13.4 AI 修改对比面板

页面形态

优先作为编辑器右侧抽屉或宽面板，不单独跳转页面，以便同时查看 A4 预览。

展示内容

当前处理的模块与条目；

优化目标；

原始内容；

建议内容；

新增、删除和替换差异；

修改理由；

事实风险提示；

本次额度信息。

操作

全部接受；

逐条接受；

拒绝；

重新生成；

补充事实后再次生成；

撤销已应用修改。

关键规则

建议生成时原文保持不变；

接受后立即反映到表单和预览；

应用修改时建立可恢复版本；

重新生成前明确额度规则；

被标记为缺乏证据的内容不能一键写入。

13.5 简历诊断

页面目标

将“这份简历哪里有问题”转换成按影响程度排序、可直接定位修复的任务列表。

诊断维度

必要信息完整性；

求职目标清晰度；

经历表达结构；

动作与结果证据；

重复、空泛和冗长表达；

时间与格式一致性；

页面长度与留白；

基础 ATS 可读性风险。

结果结构

总体结论，不夸大分数准确性；

高、中、低优先级问题；

每条问题对应具体模块和证据；

“去修改”直接定位编辑器；

已修复项可自动或手动标记完成。

13.6 JD 匹配面板

输入

JD 文本粘贴；

可选岗位名称、公司和语言；

明确提示用户避免粘贴敏感个人信息。

输出

岗位核心职责；

硬技能与软技能；

关键词；

简历中的匹配证据；

可强化内容；

未被证明的要求；

建议优先级。

交互

点击建议定位到具体简历条目；

进入 AI 修改对比后再应用；

用户可以标记某项“不适用”；

保存 JD 与简历版本的关联，便于后续回看。

13.7 模板选择

初期提供少量高质量、ATS 友好的模板；

每个模板展示缩略预览、适用场景和版式特点；

切换前后内容不变；

模板之间共享统一内容模型；

会员模板需清楚标识，但不能干扰浏览；

当前模板、试用模板和已购买权益状态明确区分。

13.8 导出与分享

导出弹窗

当前文件名；

PDF 格式；

页数和分页提醒；

权益状态；

生成按钮与任务状态；

成功下载入口；

失败原因和重试。

分享弹窗

开启或关闭链接；

复制链接；

可选有效期；

可选是否允许下载；

显示当前访问状态；

关闭后旧链接立即失效。

MVP 的分享能力应是只读预览，不允许访客编辑原简历。

13.9 会员方案

页面目标

清楚解释为什么需要付费、付费后获得什么，以及额度如何使用。

页面内容

当前套餐和到期状态；

免费版与会员版权益；

AI 次数或额度说明；

PDF、模板、JD 等功能限制；

套餐价格；

购买按钮；

常见问题；

退款与权益说明入口。

设计原则

不使用故意模糊限制的文案；

不默认勾选自动续费；

价格、周期、总金额和权益生效时间必须明确；

当前未确定的价格、额度数量和模板数量统一标记为 TBD。

13.10 登录注册、支付结果与个人中心

登录注册

页面简洁，只保留认证所需内容；

登录后回到用户原先想访问的位置；

错误反馈区分账号问题、验证问题和网络问题。

支付结果

成功：订单信息、已生效权益、返回编辑器；

处理中：允许刷新或稍后查询，不重复扣款；

失败：说明状态和重新支付入口；

支付成功但权益未生效：提供自动查询和支持入口。

个人中心

基本账户信息；

当前会员与额度；

使用量记录；

订单记录；

安全与隐私；

账号注销与数据删除入口。

14. 简历内容模型

14.1 模块与字段建议

模块

核心字段

说明

基本信息

姓名、头像、电话、邮箱、城市、链接

联系方式可控制显示

求职意向

目标岗位、行业、城市、工作类型

用于定位和 JD 建议

职业概要

标题、简介

更适合有工作经验用户

教育背景

学校、专业、学历、时间、课程/成绩

成绩和课程可选

工作经历

公司、职位、地点、时间、职责成果

支持多条要点

实习经历

公司、岗位、时间、职责成果

在职用户可隐藏

项目经历

项目名、角色、时间、背景、行动、结果、技术

支持课程/个人/商业项目

校园经历

组织、角色、时间、活动与结果

可选模块

技能与证书

技能、熟练度、证书、取得时间

熟练度避免夸大

奖项荣誉

名称、级别、颁发方、时间、说明

可选模块

自我评价

简短总结

提示避免空泛套话

自定义模块

标题、条目和描述

受统一版式约束

14.2 默认模块组合

用户阶段

默认重点模块

默认弱化/隐藏模块

在校/应届

教育、项目、实习、校园、技能

工作经历、职业概要

在职

职业概要、工作、项目、技能

校园、奖项、实习

待业/转岗

职业概要、工作/项目、技能、求职意向

根据实际经历决定

默认隐藏不等于删除，用户可随时添加和恢复。

15. 全局交互与状态规范

15.1 操作反馈矩阵

场景

进行中

成功

失败

自动保存

正在保存

已保存

保存失败，点击重试

AI 优化

骨架/进度与可取消提示

展示建议，等待确认

返还额度，允许重试

JD 分析

正在解析

展示证据和建议

保留输入，允许重试

PDF 导出

生成中

下载可用

显示原因，允许重试

分享链接

创建中

复制成功

不改变旧链接状态

支付

跳转/处理中

权益已生效

明确订单状态与后续动作

删除

确认后处理中

从列表移除

保持原对象并提示失败

15.2 空状态原则

空状态必须说明：

当前为什么是空的；

用户下一步可以做什么；

是否提供示例或创建入口。

避免只放插画和“暂无数据”。

15.3 错误原则

错误信息描述事实和恢复动作；

不使用“系统异常”作为唯一说明；

用户输入不得因请求失败而消失；

可以重试的任务保留原参数；

权限错误、额度不足和网络错误必须区分。

15.4 撤销与版本

文本编辑支持撤销/重做；

AI 修改应用后可撤销；

关键批量操作创建版本；

历史版本恢复前提示当前未保存内容；

版本恢复是创建新版本，而不是抹除历史。

16. AI 产品规则

16.1 输入边界

AI 可使用：

用户填写的结构化简历内容；

用户明确提供的补充事实；

用户粘贴的目标 JD；

用户选择的优化目标和语言。

AI 不应使用：

未经用户同意的私有外部数据；

从公开网络猜测出的个人经历；

与当前任务无关的其他用户数据。

16.2 输出规则

返回结构化结果，而非不可控整篇覆盖；

修改以字段级或条目级变更表达，可采用 JSON Patch；

每条建议包含修改内容、原因和证据状态；

无法确认的内容使用提问，不生成确定陈述；

保留原文和请求记录以支持对比与审计。

16.3 禁止行为

编造项目规模、增长率、节省成本等数字；

添加用户未声明的技能、证书或语言能力；

夸大职位、职责或管理范围；

虚构学校、公司、奖项或时间；

为提升匹配度而偷偷复制 JD 关键词；

未经确认直接覆盖简历。

16.4 额度规则

请求开始前检查并预占额度；

请求成功后确认扣减；

服务失败或无有效结果时返还预占额度；

重复请求使用幂等标识，不能重复扣减；

用户主动重新生成是否再次扣减，必须在操作前明确说明；

使用量在个人中心可查询。

17. 商业化与权益

17.1 套餐结构建议

层级

作用

可能包含的权益

免费版

让用户完成基本价值验证

基础编辑、少量模板、有限 AI/JD、有限导出

会员版

面向持续求职和多版本用户

更多 AI/JD、会员模板、更高导出与版本权益

AI 次数包

面向低频但临时有需求用户

额外 AI 或 JD 分析额度

具体价格、额度、周期、模板数量和退款条件均为 TBD，需要通过成本测算和用户访谈确定。

17.2 权益判断原则

前端展示只能作为提示，最终以服务端权益判断为准；

权益不足时保留用户当前操作上下文；

购买成功后返回原任务继续，不要求用户重新输入；

会员过期不删除用户已有简历；

退款后按规则回收未使用权益，并保留必要订单记录；

支付回调和权益激活必须幂等。

18. 隐私、安全与可访问性

18.1 隐私与安全

简历默认私有；

用户只能访问自己的内容；

分享必须由用户主动开启，并可随时关闭；

文件下载使用临时授权链接；

敏感日志不得记录完整简历正文、支付凭证或认证信息；

明确说明 AI 数据的用途、保存周期和删除方式；

提供账号注销和个人数据删除流程；

关键操作保留必要审计记录。

18.2 可访问性

图标按钮必须有可读名称和 Tooltip；

键盘可访问顶部操作、侧栏导航和表单；

焦点状态不能仅依靠颜色；

深色界面文字和控件满足基本对比度；

表单错误同时提供文字说明；

拖拽排序应提供键盘或按钮替代方式；

不以颜色作为 AI 差异的唯一表达。

18.3 设备策略

MVP 以 1440px 桌面端为主要设计画布；

支持常见笔记本宽度，必要时自动收起一侧面板；

小屏设备可查看和进行轻量修改，但不承诺完整三栏编辑体验；

手机端首期以查看、分享和下载为主。

19. 原型设计交付要求

19.1 第一轮原型

优先完成：

简历编辑器默认状态；

左侧面板收起状态；

右侧面板收起状态；

模块显隐与拖拽排序；

AI 修改前后对比；

JD 匹配结果；

分享弹窗；

PDF 导出弹窗；

自动保存失败；

简历工作台空状态与正常状态；

创建简历；

会员方案与权益不足提示。

19.2 Figma 文件组织建议

页面

内容

00 Foundations

引用 design.md 的颜色、字体、间距和网格

01 Components

按钮、输入、图标栏、Tooltip、折叠项、弹窗、状态

02 Editor

编辑器及主要交互状态

03 Dashboard

工作台和创建流程

04 AI & JD

AI 对比、诊断和 JD 匹配

05 Export & Billing

导出、分享、会员和支付状态

99 Archive

被替换方案，不与当前版本混用

19.3 画板命名

使用“页面 / 场景 / 状态”格式，例如：

Editor / Basics / Default

Editor / Layout / Reordering

Editor / AI Compare / Result

Editor / Save / Failed

Dashboard / Resume List / Empty

Export / PDF / Generating

19.4 原型标注要求

每个关键控件标注：

显示条件；

可用与禁用条件；

点击结果；

加载反馈；

成功反馈；

失败恢复方式；

是否消耗权益；

是否改变或删除用户数据。

20. 可用性验证计划

20.1 首轮测试任务

邀请目标用户完成以下任务：

从零创建一份简历；

添加一段项目或工作经历；

隐藏不适用模块并调整顺序；

使用 AI 优化一段内容并拒绝其中一条建议；

粘贴 JD，找到一个未满足要求；

调整字体和页边距，使简历控制在合理页数；

导出 PDF；

创建并关闭分享链接。

20.2 观察重点

用户是否理解左右图标栏；

是否能发现面板收起入口；

是否把隐藏误认为删除；

是否相信 AI 对比结果；

是否理解 JD 的“未证明”与“不具备”的区别；

是否能判断保存完成；

是否能找到导出和分享；

深色工具界面是否长时间使用舒适。

20.3 通过标准

用户无需引导即可完成主要任务；

不发生内容丢失或误覆盖；

用户能准确说出 AI 建议是否已应用；

用户能恢复隐藏模块和撤销修改；

用户能理解权益不足后的下一步；

预览和 PDF 无明显差异。

21. 版本规划

版本

主要内容

验证目标

V0.1

结构化填写、保存、模块管理、实时预览

编辑主流程可用

V0.2

模板、排版、PDF 生成与导出

所见接近所得

V0.3

AI 单段优化、对比、撤销与额度

AI 可信且可控

V0.4

简历诊断与 JD 匹配

岗位针对性有价值

V0.5

会员、权益、订单和支付

商业闭环成立

V1.0

稳定性、隐私、运营与公开测试

可对外持续运营

后续

导入、更多模板、多语言、论文模块

扩展用户场景

22. 待决策事项

以下项目不应由设计或开发自行假定：

决策

需要回答的问题

建议依据

品牌名称

产品最终叫什么？

域名、商标、用户记忆测试

免费权益

免费用户能导出几次、用多少 AI？

模型成本与转化测试

会员价格

月度、季度或买断？

成本、竞品和访谈

自动续费

是否首期支持？

支付合规与运营能力

模板数量

首发提供几套？

先保证质量和岗位覆盖

分享权限

是否支持密码、有效期、禁止下载？

隐私风险与实现成本

导入时间

PDF/Word 导入何时进入版本？

解析准确率与用户需求

JD 保存

默认保存还是用户选择保存？

隐私预期和复用价值

AI 数据保留

保存多久、是否用于改进？

合规、成本和用户信任

ATS 表述

如何避免把提示包装成确定评分？

法务与用户研究

23. MVP 验收清单

23.1 核心闭环

用户能够注册并进入工作台；

用户能够创建、重命名、复制和删除简历；

用户能够填写核心简历模块；

内容变化能够实时反映到 A4 预览；

模块能够显示、隐藏、新增和排序；

左右面板能够独立收起与展开；

字体、字号、字重、行高、颜色和边距能够调整；

系统能够稳定自动保存并处理失败；

AI 优化展示前后对比并等待用户确认；

JD 匹配能够区分已有证据和未证明要求；

用户能够切换模板而不丢失内容；

用户能够生成并下载 PDF；

用户能够创建和关闭只读分享链接；

权益不足时能够进入购买并返回原任务；

AI 或导出失败不会造成重复扣费。

23.2 质量与信任

AI 不编造经历、数字、技能和证书；

所有 AI 修改都可以拒绝和撤销；

用户只能访问自己的简历；

中文字体正常显示；

PDF 内容、分页和预览基本一致；

隐藏模块不删除数据；

分享关闭后链接失效；

支付回调和额度扣减幂等；

关键失败状态都有恢复路径；

所有页面符合 design.md 的统一视觉规范。

24. 文档维护规则

本文档记录已确认的产品规则，不用来存放临时视觉尝试；

视觉 Token 和组件外观统一维护在 design.md；

技术实现方案统一维护在 technical-selection.md；

新增核心页面或改变用户流程时，先更新本文档再进入高保真设计；

改变 AI 事实边界、付费权益、数据删除或分享权限时，必须同步更新验收标准；

未确定事项使用 TBD，不得以设计稿中的示例数字代替正式决策；

每次版本评审记录修改日期、修改人、原因和影响范围。

本 V0.1 文档的核心结论是：产品首先解决“真实经历不知道如何转化为可投递简历”的问题。编辑器、AI、JD 匹配、模板和导出共同服务于这一目标；任何新功能都应先证明它能提高内容质量、岗位针对性、编辑可靠性或用户信任，再进入产品范围。