AI 简历产品设计规范
设计方向：深色、简约、克制的专业生产力工具
适用范围：简历编辑器、简历工作台、模板中心、会员中心、AI 优化与后续新增页面
本文档为全新设计基线，不继承此前任何配色、布局或组件方案。

1. 配色方向
参考界面的高级感主要来自深色中性色，而不是大量品牌色。界面通过相近深色之间的轻微明度差异建立层级，并以白色 A4 纸张形成最主要的视觉对比。
1.1 基础配色
用途建议色值	
页面主背景	#0A0A0A
左右内容面板	#0D0D0D
顶部导航栏	#151515
输入框背景	#101010
悬浮背景	#1A1A1A
选中背景	#242424
默认边框	#2A2A2A
主要文字	#F5F5F5
次要文字	#A3A3A3
弱化文字	#737373
A4 纸张	#FFFFFF
简历正文	#171717


1.2 颜色使用规则
- 主界面不使用大面积亮蓝色。
- 黑色背景不能全部使用同一个色值，需要通过 #0A0A0A、#0D0D0D、#101010 和 #151515 建立轻微层级。
- 面板与工作区主要通过 1px 深灰边框区分，不通过明显阴影区分。
- A4 纸张始终使用纯白色，不能跟随深色主题改变。
- 主要文字使用灰白色而不是纯白色，降低长时间编辑时的视觉疲劳。
- 品牌色只用于极少数关键状态，例如焦点边框、AI 状态和主操作。
- 状态色只能小面积使用，不能成为卡片或面板的大面积背景。
- 同一页面最多只允许一个明显的主操作按钮。
1.3 强调色原则
产品不预设高饱和品牌色。默认主操作优先采用白底黑字；如果后续确定品牌色，只能用于：
- 当前焦点；
- AI 状态标记；
- 链接；
- 成功、警告和错误反馈；
- 页面唯一的关键操作。
禁止使用：
- 大面积亮蓝色按钮；
- 蓝紫渐变；
- 霓虹光效；
- 彩色阴影；
- 玻璃拟态；
- 大面积彩色卡片。
2. 布局方向
采用与参考界面一致的沉浸式全屏编辑器。中间是 A4 简历预览，左侧负责内容编辑，右侧负责模板和样式设置，两侧最外层为窄图标导航栏。
flowchart LR
    A["左侧图标栏"] --> B["内容编辑面板"]
    B --> C["A4 预览工作区"]
    C --> D["设置面板"]
    D --> E["右侧图标栏"]
2.1 建议尺寸
区域建议尺寸	
顶部导航栏	52px
左侧图标栏	48px
左侧编辑面板	420–520px
中间预览区	自适应填充
右侧设置面板	340–380px
右侧图标栏	48px


2.2 关键规则
- A4 纸张始终在中间预览区居中。
- 中间工作区占据最大视觉面积。
- 左右面板通过细边框区分，不使用大卡片包裹。
- 左右内容面板可以独立收起。
- 面板收起后，只保留对应的窄图标栏。
- 左右面板收起或展开后，中间预览区自动重新分配空间。
- A4 纸张根据可用空间调整缩放比例，但不改变真实纸张比例。
- 底部缩放工具悬浮在预览区，不占用顶部导航栏。
- 页面中不放置大量白色卡片。
- 每个面板拥有独立滚动区域，顶部导航栏保持固定。
2.3 顶部导航栏
顶部导航栏只保留全局功能：
- 返回工作台；
- 当前文件名称；
- 自动保存状态；
- 撤销；
- 重做；
- 分享；
- 导出。
模板、字体、颜色、行高和页面边距等局部功能放在右侧设置面板中，不放进顶部导航栏。
2.4 左侧区域
- 最左侧图标栏负责在基本信息、教育背景、项目经历等内容模块之间快速跳转。
- 左侧编辑面板只负责当前简历内容的填写与编辑。
- 模块为空或被隐藏时，不在 A4 简历中显示。
- 左侧面板底部保留“新增模块”和“模块排序”入口。
2.5 右侧区域
- 最右侧图标栏负责模板、布局、分享、排版、设计、页面、AI 优化、JD 匹配和导出等工具切换。
- 右侧设置面板只展示当前选中工具的内容。
- 设置项优先采用折叠面板结构，默认只展开一个主要区域。
2.6 响应规则
- 宽屏状态下，左右内容面板可以同时展开。
- 空间不足时，优先收起右侧设置面板。
- 进一步缩小时，再允许用户收起左侧内容面板。
- 两侧窄图标栏始终保留。
- 当前阶段以桌面端宽屏为主，不为手机端强行压缩编辑器结构。
3. 字体方向
界面统一使用：
font-family: Inter, "Noto Sans SC", sans-serif;
- 中文优先使用 Noto Sans SC。
- 英文、数字、日期和金额优先使用 Inter。
- 普通界面不使用衬线字体、手写字体或装饰字体。
- 简历纸张内部字体可以由不同模板单独定义。
3.1 字体层级
内容字号字重行高			
顶部文件名	14px	600	20px
面板主标题	20px	650	28px
折叠模块标题	17px	600	24px
输入框标签	12px	500	18px
输入内容	13px	400	20px
辅助文字	12px	400	18px
Tooltip	12px	500	18px


3.2 字体使用规则
- 不使用过大的页面标题。
- 不使用过多粗体。
- 同一区域最多出现三个文字层级。
- 主要通过字号、字重、文字灰度和留白建立信息层级。
- 标题使用主要文字色，正文使用次要文字色，说明信息使用弱化文字色。
- 不使用彩色标题制造层级。
- 不在工具型页面使用营销网站式的大标题和大段宣传文案。
- 英文标题不使用全大写形式。
4. 组件风格
4.1 输入框
- 高度约 34–38px，默认使用 36px。
- 背景使用 #101010。
- 边框使用 1px solid #2A2A2A。
- 圆角只使用 4–6px，默认使用 4px。
- 输入文字使用 #F5F5F5。
- 占位文字使用 #737373。
- 悬停时边框轻微变亮。
- 聚焦时边框变亮，但不出现蓝色发光效果。
- 不使用明显阴影。
- 输入框标签放在输入框上方，保持 6px 间距。
4.2 按钮
- 高度约 34px。
- 圆角使用 4–6px。
- 默认按钮使用透明或深灰背景。
- 次按钮使用深色背景、灰白文字和 1px 边框。
- 主按钮可使用白底黑字。
- 图标与文字之间保持 6–8px 间距。
- 按钮水平内边距约 12px。
- 不使用大面积蓝色或渐变按钮。
- 不使用明显投影。
- 同一页面只突出一个主按钮。
4.3 折叠面板
参考界面中的 Template、Layout、Sharing 和 Typography：
- 不放进独立圆角卡片。
- 每一项直接排列在面板中。
- 使用细分隔线分隔不同项目。
- 左侧放置展开箭头和功能图标。
- 标题使用白色或灰白色半粗体。
- 点击整行展开或收起。
- Hover 时只出现轻微的深灰背景。
- 默认只展开一个主要项目。
- 展开内容直接位于标题下方，不再套第二层大卡片。
4.4 标签
- 使用深灰背景。
- 使用白色或灰白色小号文字。
- 圆角约 10–12px。
- 标签之间保持 6–8px 间距。
- 标签高度保持紧凑。
- 不使用多种彩色标签。
- 不添加渐变和阴影。
4.5 图标栏
- 图标尺寸使用 16–18px。
- 点击区域使用 40–44px。
- 图标水平居中。
- 默认图标使用灰色。
- Hover 时图标变白，并出现轻微深灰背景。
- 当前项使用浅灰或深灰选中背景，不使用蓝色大方块。
- 图标下面不显示文字。
- 鼠标悬浮时显示 Tooltip。
- 相关功能可以分组，不必机械地铺满整列。
- 同一产品只使用一套线性图标风格。
4.6 卡片
- 卡片不是默认容器，只在内容确实需要独立分组时使用。
- 背景使用 #101010 或 #151515。
- 边框使用 1px solid #2A2A2A。
- 圆角使用 4–6px。
- 默认不使用阴影。
- Hover 时只轻微改变背景或边框。
- 禁止使用大圆角、彩色背景和悬浮式重阴影。
4.7 A4 纸张
- 使用纯白背景 #FFFFFF。
- 保持 210:297 的真实比例。
- 简历正文使用 #171717。
- 纸张圆角不超过 2px。
- 允许使用轻微黑色阴影，让纸张与深色工作区分离。
- 纸张本身不跟随深色主题。
- 所有编辑结果必须实时反映在 A4 预览中。
4.8 底部缩放工具
- 悬浮在中间预览区底部居中位置。
- 使用深灰背景和细边框。
- 控件高度约 34–36px。
- 可以使用胶囊形圆角，但不得使用彩色背景。
- 只包含缩小、当前比例、放大、适配页面等预览相关功能。
- 不与顶部导航栏重复放置同一功能。
4.9 左右面板收起按钮
- 左右内容面板分别拥有一个独立的收起按钮。
- 收起按钮固定在内容面板靠近预览区的一侧。
- 使用深灰背景、细边框和小圆角。
- 左右按钮使用镜像图标表达收起方向。
- 收起后按钮图标变为展开方向。
- 收起后保留对应的窄图标栏。
5. 高级感的关键
参考界面好看的原因并不是简单地“用了黑色”，而是它同时做到了：
- 控件尺寸紧凑；
- 几乎没有无意义装饰；
- 边框非常克制；
- 圆角很小；
- 没有大面积品牌色；
- 信息层级主要依靠字体和间距；
- 左右面板自然融入背景；
- 白色 A4 简历形成强烈焦点；
- 操作区域与预览区域职责清晰；
- 所有组件都像专业工具，而不是营销网站；
- 重要操作清晰，但不会与内容争抢注意力；
- 页面允许存在适量空白，不为了填满空间添加无用内容。
5.1 判断页面是否高级
页面交付前必须确认：
- 去掉品牌色后，层级是否依然清晰？
- 去掉阴影后，面板是否仍能通过背景和边框区分？
- 页面是否存在多个同时抢眼的按钮？
- 是否使用了不必要的大圆角？
- 是否添加了没有实际作用的卡片或装饰？
- 白色 A4 是否仍然是编辑器的第一视觉焦点？
- 图标、文字、边框和间距是否具有稳定节奏？
6. 后续原型的统一方向
后续的简历工作台、创建简历、模板中心、会员中心、AI 优化和 JD 匹配页面都必须使用同一套视觉语言：
- 使用深色背景；
- 以黑、深灰、灰白为主；
- 使用小圆角；
- 使用细边框；
- 使用紧凑控件；
- 只在必要的浮层和 A4 纸张上使用少量阴影；
- 只使用少量强调色；
- 不使用渐变光效；
- 不堆叠大量卡片；
- 不放置没有实际作用的装饰；
- 不生成过度完整、对称和花哨的“AI 概念界面”；
- 不为了展示 AI 而添加发光、星星、魔法棒或紫色渐变；
- 不照搬编辑器三栏结构到所有页面，应根据页面任务选择布局；
- 页面始终优先展示用户当前任务和主要内容。
6.1 简历工作台
- 深色页面背景。
- 白色 A4 缩略图作为卡片视觉主体。
- “新建简历”是页面唯一主操作。
- 编辑、复制、重命名、删除和导出等次要操作默认弱化。
- 空状态只使用简短文字和一个主按钮，不使用大插画。
6.2 模板中心
- 使用规整但克制的模板网格。
- 模板预览保持白色纸张视觉。
- 分类和筛选采用紧凑 Tabs、标签或下拉框。
- 选中模板只使用浅色边框，不使用彩色卡片背景。
6.3 会员中心
- 套餐数量保持克制。
- 推荐套餐只通过边框和小标签强调。
- 不使用金色光效、紫色渐变和夸张价格动画。
- 权益对比使用清晰列表和细分隔线。
6.4 AI 优化与 JD 匹配
- AI 功能使用与普通工具一致的组件语言。
- AI 强调色只能小面积出现。
- 原文、建议结果和修改理由具有清晰层级。
- 用户确认之前不得直接覆盖原文。
- 接受、拒绝和重新生成的操作层级清晰。
6.5 新页面生成约束
以后生成任何原型前，必须先读取本文件，并在任务中明确：
严格按照 design.md 设计。使用深色中性色、紧凑字体、小圆角、细边框和弱阴影；禁止亮蓝色大按钮、彩色渐变、玻璃拟态、大圆角卡片、无意义装饰以及过度对称的 AI 概念界面。

如果新页面确实需要偏离本规范，必须先说明原因并更新 design.md，不能在单个页面中临时发明新的视觉语言。