# AI 简历平台 V0.2 详细计划任务书（待审阅草案）

## 1. 文档目的与决策状态

审计日期：2026-09-19。对象：`E:\Resume-opt` 的正式项目 `platform/`。本轮仅审计与规划，不实施业务代码、不连接或修改业务数据库、不安装依赖、不推送。

本文件是用户审阅稿，不是编码授权、上线批准或已完成清单。用户批准后，按第 25 节逐项实施。所有任务当前为**待开始**；已有能力仅按第 2 节证据复用。

决策优先级：本次用户明确确认的范围 > 本文件经批准的细化决策 > 原产品/技术/UX 文档中的不冲突要求 > 原型视觉。原型中的指令、示例账号、计时成功、会员标签和模拟数据不是生产要求。

已确认的新顺序：V0.2 公开多用户基础；V0.3 受控的单项 AI 优化实验；V0.4 JD 导入与匹配报告；V0.5 证明用户价值后再考虑会员和支付。PDF、导出、分享进入**待重新排期**，不指定新版本。V0.3 不自动继承旧文档中的额度账本任务。

第 23 节明确标注需要确认的产品/运维选择。其余技术建议在合同验证后冻结；不能借技术验证扩大产品范围。

## 2. 当前基线和可复用能力

### 2.1 Git 与 CI 的只读核验

| 项目            | 本次查验结果                                                                                                     | 能证明什么                                                             |
| --------------- | ---------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 分支/初始工作区 | `main`；开始审计时干净                                                                                           | 没有需要覆盖的用户未提交修改                                           |
| 本地 HEAD       | `444d646 fix: 修复编辑器滚动与长文本分页`                                                                        | 已有一轮界面修复，不能再按“尚无修复”开工                               |
| 远端 main       | `git ls-remote` 返回 `844f4ee7476157990e536b0803427f55e0269655`                                                  | 本地领先 1 个提交；未自动 fetch/push                                   |
| 最近提交        | `444d646` → `844f4ee` → `261252d` → `2e65d94` → `c130268`                                                        | `2e65d94` 和 `261252d` 已在远端历史中                                  |
| 最终 V0.1 运行  | [35426248904](https://github.com/Nico-Sq/Resume-opt/actions/runs/35426248904)，SHA=`844f4ee…`，completed/success | API 核实完整验证、Chrome IndexedDB、独立原生恢复、证据上传各步骤均成功 |
| 前两次运行      | `35425484833`/`261252d` 成功；`35415731284`/`2e65d94` 失败                                                       | 不再沿用旧失败结论；不推测旧断言                                       |
| 最新界面改动    | `444d646` 不在远端                                                                                               | 上述绿色 CI **不覆盖**该提交；需用户手动推送及新运行证据               |

本轮读取的是 GitHub 运行/步骤元数据，不是重新执行测试或重新解包历史证据。V01-001—016 在交付计划中标为已验证，V0.1 受控本地/内网候选版有 CI 支持；不能据此宣称当前 HEAD 全部验证、公开发布就绪或用户已批准 V0.2。

### 2.2 已读资料与代码证据

已交叉阅读根 `AGENT.md`、`platform/apps/web/AGENTS.md`，以及 `product-design.md`、`design.mf`、`technical-selection.md`、`ux-interaction-spec.md`、`v0.1-delivery-plan.md`、`README.md`；辅助读取 `platform/database/README.md`、保存可靠性实验及原型 README。正式改动继续遵守 Next 包内版本对应指南；当前 Next 为 16.3.5，不复制旧版中间件/缓存写法。

以下路径均相对仓库根，作为实现时的导航，不表示这些文件本轮被修改。

| 能力           | 实际代码/缺口                                                                                              | V0.2 处理                                                                                       |
| -------------- | ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 身份端口       | `platform/packages/application/src/ports.ts` 的 `IdentityProvider.requireIdentity()`                       | 保留应用端口；每次请求注入受信身份，不缓存请求身份到全局                                        |
| 固定身份       | `packages/infrastructure/src/identity/fixed-identity.ts`；`apps/web/src/server/runtime.ts` 只装配 fixed    | Better Auth 只有枚举/端口预留，包尚未安装，不能称接入完成                                       |
| 环境           | `packages/config/src/server.ts` 禁止 staging/production fixed，但三个 PG URL 均必填且数据库名写死 dev/test | 拆分运行、测试、迁移配置；不能把开发库连接原封不动上生产                                        |
| 数据库         | `database/migrations/0001_initial.up.sql`：6 张业务表，加迁移元数据共 7 表                                 | 追加迁移，不修改 0001 校验和                                                                    |
| RLS/事务       | `postgres-user-transactions.ts` 同连接 BEGIN、事务局部 `app.user_id`、owner 条件、COMMIT/ROLLBACK          | 延续；补账户停用、删除及新 CRUD 路径，不用超级用户证明隔离                                      |
| 保存           | 完整快照、强 ETag、revision CAS、快照/审计/7 天回执同事务                                                  | 复用，不重写第二套保存协议                                                                      |
| 文档           | `packages/domain/src/resume/schema.ts`、`default.ts`、`commands.ts`                                        | 稳定 UUID、严格结构、显隐/排序保正文；V0.2 不升级内容 schemaVersion                             |
| 草稿           | `client/editor/persistence/*`：`[userId+resumeId+tabId]`，pending/ACK/救援、账户清理阻断                   | 存储内核已有；真实 Session 切换和退出 UI 尚未贯通                                               |
| 协调器         | `client/editor/saving/save-coordinator.ts` 有 auth-paused；start 可直接重放 pending                        | 真实身份复核必须发生在重放前；“已有 auth-paused”不等于可安全换账户                              |
| 入口           | `/` → `EditorEntry` → `/api/v1/editor-bootstrap`，读取固定 ID 和 `X-Local-Draft-Account`                   | 增加动态编辑路由；服务端返回草稿分区 ID 仅用于客户端分区，不作请求授权                          |
| 已暴露业务 API | bootstrap、GET resume、PUT document；另有 live/ready                                                       | 当前无工作台 CRUD、Auth、versions/restores Route Handler；文档中的历史恢复 API 不能当作已有实现 |
| UI             | `editor-workspace.tsx` 存在示例分享地址、PDF/专业版/剩余次数及 AI/JD 图标错接 style                        | 独立清理伪入口；只保留真实编辑/保存/预览与 JSON 救援                                            |
| CI             | `.github/workflows/verify.yml` 在临时 PostgreSQL 16.15 上验收，Chrome 后原生恢复 `if: always()`            | 保留恢复独立执行；新增真实 Auth 模式，不让固定模式覆盖它                                        |

未审计生产数据，没有读取 `.env.local` 密码。现有测试支持复用，但本轮不把历史单测数量写成当前重新通过的结果。

## 3. 文档冲突与处理结论

| 文档位置                                                            | 冲突/差异                                                          | 批准本计划后需同步的结论                                                                                  |
| ------------------------------------------------------------------- | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- |
| `product-design.md` §21；`technical-selection.md` §21、§17 版本门槛 | 旧 V0.2 是模板/PDF/分享，公开化偏后                                | 替换为本次新顺序；PDF/分享不指定版本                                                                      |
| 技术 §1.2、§15.4/15.7、§21                                          | V0.3 自动包含注册赠额/额度账本                                     | 商业意图保留为历史决定，不在 V0.2 建表/回调；V0.3 实验另立范围                                            |
| 产品 §13.1—13.3、§13.7—13.10；UX §6/7/11/12/17/18                   | 工作台和编辑器混有会员、导出、分享、AI                             | V0.2 页面只展示真实范围；原型不是占位入口授权                                                             |
| 技术 §14.5                                                          | 未验证邮箱可编辑；本稿建议验证后才进入私有业务                     | 第 23 节确认后统一权限矩阵，不能两套语义并存                                                              |
| 技术 §7.5                                                           | 删除返回 JobDTO，依赖 Worker                                       | 本期短事务软删除返回元数据；定时受控 CLI 做清理，不引入通用 Job/Worker                                    |
| 技术 §7/9 与代码                                                    | 文档 title 100、块 5000；当前 DB title 200、块 4000，链接含 mailto | 本期保存维持代码契约；新增标题统一 trim 后 1—200，前后端按 Unicode code points 验证；不静默改存量正文限制 |
| V0.1 §2/5 与正式 API                                                | 历史恢复/另存新简历表述超出当前暴露能力                            | 区分本地冲突救援与服务端历史恢复；V0.2 交付复制，不擅自新增完整历史浏览                                   |
| README 当前状态；V0.1 §8                                            | README 仍写门禁进行中；V0.1 写暂无新阶段                           | 记录历史完成 SHA、界面稳定性补验与已批准新路线，不抹掉旧版本边界                                          |
| 技术 §20.1/20.5                                                     | 列出大量“待交付”脚本、90 天建议证据；实际只一部分，CI 14 天        | 按第 19/21 节列真实命令；不能调用不存在的 release/security/a11y 脚本并声称成功                            |
| 多处 `design.md`                                                    | 仓库实际文件为 `design.mf`                                         | 后续统一链接；本轮不修改原文档                                                                            |

本稿对 V0.2 的范围优先规则明确，并不把互相冲突的两套 V0.2 都保留为有效定义。

## 4. 产品目标

一个新用户能验证邮箱、登录、创建多份自己的简历，编辑保存后在新会话回读；另一个用户无法读取或修改它们。会话失效、并发编辑、删除和灾难恢复不能造成跨账户上传、静默覆盖或已删除数据复活。

可测结果：两个真实注册账户走完第 19 节全矩阵；每个成功 ACK 可在所属账户回读；越权返回无内容；未同步草稿可救援；没有虚假功能入口。保留 V0.1 正确性和性能门槛。

## 5. 范围与非范围

纳入：Better Auth 邮箱密码/验证/找回/重置/退出；PG Session；账户隔离；最小工作台；创建空白/现有基础模板、列表、打开、重命名、复制、软删除及有限恢复；动态编辑入口；跨账户草稿保护；硬删除/账户注销的必要隐私闭环；安全、测试、迁移与上线证据。

最小化：只用现有 `ats-basic` 基础模板和现有文档工厂。模板创建指选择现有样式、生成空内容，不把“张三”测试经历复制给真实用户；不做模板市场、新模板引擎、上传导入。工作台无搜索，无搜索空结果状态；分页代替无限加载。保留 JSON 草稿救援，这是数据保全，不是 PDF 产品导出。

硬排除：AI 优化/Provider、JD、完整诊断、额度账本、会员、订单、支付、PDF Worker/正式导出、分享链接、对象存储、Redis、向量库、微服务、Kubernetes、团队、实时协同、手机号/SMS/微信/OAuth/SSO、管理后台、无需求的抽象层及未来空表。头像上传也不借工作台引入文件系统；当前头像资源能力不足时使用非上传式默认头像。

## 6. 角色与核心流程

| 角色            | 允许                             | 拒绝                                          |
| --------------- | -------------------------------- | --------------------------------------------- |
| 未登录          | 注册/登录/找回、协议和隐私说明   | 任何私人列表、正文、版本、草稿上传            |
| 已注册未验证    | 验证提示、受限重发、找回         | 按建议方案不能进入私有业务；不能探测其他账号  |
| 已验证且 active | 本人多简历工作台、编辑与恢复     | 他人的任何 ID、任意 ownerId                   |
| 过期/停用/注销  | 本机草稿救援、重新认证或状态说明 | 继续上传；停用/注销不能通过旧 Cookie 恢复权限 |
| 运维            | 独立受限身份运行迁移、清理和恢复 | 普通 Web 不持有运维权限，无管理后台读取正文   |

主线：注册 → 查收验证 → 登录 → 空工作台 → 创建 → `/resumes/:id/edit` → 本地备份 → 云端 ACK → 返回列表 → 新会话回读。

异常线：编辑 A → Session 失效 → 冻结上传并保留 A 草稿 → 同 A 重登、校验资源/revision 后原键重试；若登录 B，卸载 A 编辑上下文，B 不加载或展示 A 草稿。

删除线：保存或救援未同步内容 → 二次确认 → 软删除 → 最近删除中按期限恢复，或到期硬删除 → 恢复旧备份必须重放删除/恢复事件后才开放。

## 7. 页面与路由

| 路由                                  | 交付内容                           | 关键分支                                                       |
| ------------------------------------- | ---------------------------------- | -------------------------------------------------------------- |
| `/`                                   | Session 感知跳转，无公开营销页扩展 | 未登录 login；已验证 resumes；fixed 仅 local/test 可进既有入口 |
| `/login`、`/register`                 | 邮箱密码、通用失败反馈、协议入口   | 返回地址仅站内白名单，不接收任意 URL                           |
| `/verify-email`                       | 等待、重发、有效/失效/已消费反馈   | 邮件链接由受支持 Auth handler 消费；本页面不自研 Token         |
| `/forgot-password`、`/reset-password` | 请求重置、提交新密码               | 通用受理反馈；成功后重新登录，不自动恢复旧 Session             |
| `/resumes`                            | 空/列表/分页/加载/失败、账户菜单   | 只返回元数据，不生成正文缩略图                                 |
| `/resumes/new`                        | 名称、阶段、空白或基础模板         | 创建前离开不落库；失败保留字段                                 |
| `/resumes/[id]/edit`                  | 原编辑器动态装配                   | 401、统一404、本人损坏、412、离线、草稿不可用                  |
| `/resumes/trash`                      | 本人软删除元数据/剩余期限/恢复     | 不预览已删除正文、不增加永久删除按钮                           |
| `/account`                            | 最小账户说明、退出、注销确认       | 不做个人中心大改、邮箱修改、设备管理或订阅管理                 |
| `/privacy`、`/terms`                  | 经批准条款                         | 未批准不可宣称发布就绪                                         |

所有私有页服务端验权；layout 中检查不能代替 API 用例检查。动态 ID 是资源选择，不是身份证明。刷新、直接打开、浏览器后退、预取/RSC 请求都单独验证。开发 bootstrap 在真实 Auth 模式下拒绝访问，不暗中回退 fixed。

## 8. API 变更清单

下面是待实现合同，非现有端点声明。保留 `/api/v1`、成功 `{data,traceId}` 和既有错误形状；Auth 沿用锁定 Better Auth handler 的实际协议，做快照测试而不是改成自研密码 API。

`I`=UUID Idempotency-Key；`R`=现有强 If-Match。写操作服务端绑定 Session 用户；额外 ownerId/userId/status 一律拒绝。成功读写私有响应及错误均 `private, no-store`。分页默认20、最大100，按 updatedAt DESC/id DESC 稳定游标，游标与当前账户/列表类型绑定。

| 接口                         | 请求/响应与事务                                                                   | 实现位置                                                     |
| ---------------------------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| `/api/auth/[...all]`         | 注册、邮箱登录、Session、退出、验证、找回/重置；只启用所需端点                    | web Auth handler + infrastructure Auth                       |
| `GET /api/v1/session`        | 最小 `{userId,displayName,emailVerified}`，无 Session Token；供草稿分区与恢复校验 | web/server；no-store                                         |
| `GET /api/v1/csrf`           | 与服务端 Session 绑定的 CSRF 防伪令牌，不是登录凭据                               | web/server                                                   |
| `GET /resumes`               | 元数据分页；无正文/隐藏模块/其他账户统计                                          | 本人事务                                                     |
| `POST /resumes`              | `{title,stage,mode:'blank'或'basic-template'}`，I；201 当前 DTO，revision=1       | 初始 resume/version/回执/审计同事务                          |
| `GET /resumes/:id`           | 延续现有 DTO/ETag；仅所属 active 资源                                             | 替换身份装配，不重写读取                                     |
| `PUT /resumes/:id/document`  | 原正文+clientSeq、I/R；原 SaveReceipt 不改                                        | 复用保存服务；补 Session/CSRF                                |
| `PATCH /resumes/:id`         | `{title}`，I/R；200 元数据+versionId、新 ETag                                     | 重命名同一 revision+1、正文快照不变                          |
| `POST /resumes/:id/copies`   | `{sourceRevision,title}`，I；201 新 DTO                                           | 只复制当前已保存 revision；不相等412，用户刷新确认           |
| `DELETE /resumes/:id`        | `{confirm:true}`，I/R；200 `{id,revision,deletedAt,restoreUntil}`                 | 短事务软删除，不返回虚构 Job                                 |
| `GET /resumes/trash`         | 本人已删除元数据分页                                                              | 与动态 `:id` 路由不冲突                                      |
| `POST /resumes/:id/undelete` | `{}`，I/R；200 元数据+新 ETag                                                     | 期限内恢复；不是历史版本 restores                            |
| `DELETE /account`            | 重新认证+`{confirm:true}`+I；202 `{requestId,status:'accepted'}`                  | 持久化注销请求、业务停用并撤 Session；清理进度不靠浏览器存活 |

版本历史 API 当前未暴露，本期不为了“看起来完整”新增历史浏览/恢复页面。测试通过真实数据库核验版本归属、不可变与 CAS。若审计发现存在额外已暴露历史端点，则纳入同一权限矩阵，不能漏测。

重放优先顺序：认证/CSRF → 当前所有权与生命周期 → 原 key/hash 回执 → CAS → 新事务效果。新 CRUD 回执只保存安全元数据/资源引用，不保存整份正文；新建/复制重放返回引用时再次读取授权当前 DTO，并说明不是重置回旧内容。已硬删资源不能凭旧回执复活。

## 9. 数据模型与迁移

### 9.1 唯一身份决策（推荐冻结方案）

现有 `users` 没有 email/password 列且有 owner RLS。推荐新增 Better Auth 所需 `auth_users`，其 UUID 主键同时是 `users.id`，以 `auth_users.id → users.id` 的唯一外键建立 **1:1 共享 ID**；不新建任意字符串→业务 ID 映射表，不改任何存量简历 owner。

理由：直接把 `users` 用作 Auth 表需要放开登录前全局邮箱查询的 RLS、补必填邮箱并处理无邮箱测试用户；分离一张 Auth 标准用户表能保留既有业务权限边界。代价是一对一创建必须原子，不能靠两个异步注册回调碰运气。

实施方案：Auth 使用服务器 UUID 生成；`auth_users` INSERT 的受限 BEFORE trigger 在同一数据库事务插入对应 `users(id,status='active',display_name)`，随后 Auth 行外键落地；任一步失败均回滚。重复业务 ID 必须失败，不 `ON CONFLICT DO NOTHING` 接管旧用户。函数固定 search_path、限定插入字段、撤销 PUBLIC EXECUTE、只允许迁移角色管理；Auth 角色无直接业务表写权限。若锁定版本支持可证明同一 tx 的标准适配器事务，可替代该窄触发器，但必须在 V02-006 冻结一种，不能同时维护两种。

`auth_users` 承载 email/verified/name 及库要求字段；业务 display_name 只初始化，本期无资料编辑双向同步需求。`auth_sessions`、`auth_accounts` FK 到 auth_users；验证表按照实际库模型管理，不假定所有验证记录必有 userId。email 规范化采用库受支持行为并测试大小写唯一，不自造 Gmail 点号/加号合并。密码仅由 Better Auth 存为其 credential account 哈希。

旧 fixed users 保持原 UUID/简历/快照，不自动生成 Auth 账号、不用简历联系方式认领账号、不迁给首次注册者。公开部署不导入本地测试数据。若日后确需迁移用户自己的固定简历，单独明确授权并通过本人导入/复制流程；本期不加入隐式认领机制。

Better Auth 官方支持表/列映射、UUID 及标准核心表；具体适配器版本及生成 DDL 必须在 V02-006 审查锁定，本稿没有安装或验证某个版本。[数据库文档](https://better-auth.com/docs/concepts/database)

### 9.2 最小数据库增量

| 对象                 | 增量与约束                                                                                                              |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Auth 标准四表        | auth_users/sessions/accounts/verifications；共享 ID 外键、email/token/credential 唯一键、过期索引按锁定库生成并审查     |
| Auth 限流            | 启用库 PostgreSQL 持久限流所需最小表；并发计数必须实测，不默认认为库适配器原子                                          |
| resumes              | 保留 deleted_at；新增 restore_until（期限冻结）；必要的本人删除列表索引、活跃列表时间+id索引；仍受复合 owner FK         |
| resume_versions      | 不改历史内容；必要时扩 reason 到 rename/delete/undelete；删除/恢复亦递增同一 revision 并写生命周期快照，旧客户端自然412 |
| idempotency_requests | 补显式 resource_id/resource_type 供删除清理，不从 response JSON 猜资源；旧回执受控回填；保持原唯一键和保存 hash 规则    |
| privacy_requests     | 仅账户注销：请求 ID、userId、requestedAt、状态/重试错误码，无正文；用户范围 RLS；单账户单活动注销请求                   |
| deletion_events      | 仅软删/恢复/硬删/注销的有序最小事件及策略版本，支持恢复后反复应用；不含标题、正文、邮箱/Token；不是通用事件总线         |
| 审计                 | 沿用 audit_events，白名单元数据；清理时按批准策略去标识/到期删除，不能因无 FK 漏掉                                      |

用户业务角色无删除快照权限；仅限定清理角色可硬删除。Auth 角色、业务角色、迁移/清理/备份角色权限分开，无 Web owner/BYPASSRLS。所有新表必须说明角色授权，不把“RLS 已开”当答案。

新增迁移从 0002 开始，每项带验证与回滚说明，schema.ts 同步 SQL；现有 schema.ts 和 SQL 有字段类型表达差异（schema_version）需在涉及迁移时核对，不趁机重写全部 ORM。无需改变 ResumeDocumentV1/rendererVersion 来表示登录上线。

## 10. 身份、Session、Cookie 与邮件

固定身份和真实身份由**进程启动配置**二选一，不能由 header/query/cookie 切换。local/test 可显式使用任一种；staging/production 缺真实 Auth、可信 origin、密钥、PG 或邮件配置即启动/就绪检查失败。真实模式不得回退 fixed，开发 bootstrap/seed/reset 也必须拒绝。

推荐 Session 工程基线：PG 有状态 Session，7 天绝对有效期、不自动无限滑动；敏感操作需最近10分钟重新认证；退出撤当前 Session，重置密码撤全部旧 Session，注销立即使所有业务请求失权。禁用 Cookie Session 缓存、客户端 JWT 和跨子域共享。HttpOnly、Secure（staging/prod 强制 HTTPS）、SameSite=Lax、Path=/、host-only；local HTTP 的 Secure 例外仅 local/test。Session Token 不进入 JSON、JS、localStorage、IndexedDB、URL、日志；必须检查库默认 Session 返回值并做安全投影，不能假定标准 get-session 响应已满足本项目要求。

重置撤会话不是默认能力声明：显式设置库支持的 `revokeSessionsOnPasswordReset` 并测试。邮箱验证/密码重置采用库 Token 机制；建议有效期分别24小时/30分钟，一次消费，重复/过期/篡改失败。验证后跳到登录，不在邮件预取时悄悄替换当前账户。[邮箱密码文档](https://better-auth.com/docs/authentication/email-password)、[Session 文档](https://better-auth.com/docs/concepts/session-management)

注册/找回/重发对存在和不存在邮箱返回相同可见状态与通用文案，不回显“已注册/未注册”；登录统一“邮箱或密码不正确”，已掌握正确凭据的未验证分支可提示验证。测试响应字段/状态/邮件行为与时间分布，不承诺网络级绝对恒时。邮件发送失败不撤销已创建身份，可安全重发；fake 收件成功不代表实际送达。

邮件服务供应商、域名和发件身份未决定，本稿不选供应商。开发/CI 使用受控内存 Fake；发送动作必须被 await、设置超时并处理失败，不用 fire-and-forget 或开发 After delay 假成功。无需 Worker；邮件短调用在 DB 事务之外，失败通过用户受限重发恢复。公开环境要有真实接收、反垃圾/退信、发件域 SPF/DKIM/DMARC（按服务商支持）与域名校验证据。

## 11. 账户隔离、RLS 与缓存

业务事务只使用 Session 推导的 userId，URL/body 中任何“身份”不可信。每次读取/写入都验证 active 且 deleted_at 为空；仅前端隐藏按钮或 Next 路由跳转不构成授权。

| 路径/角色          | 强制隔离规则                                                                                                                                                                                                             |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| resumes / versions | owner RLS+应用 owner；versions 的 SELECT 要求父 resume 未删除；生命周期 INSERT 策略单独允许本人软删事务记录快照，不能机械把 SELECT 条件套到 INSERT；source_version_id 增加同 resume 复合外键及所需唯一键，禁止跨简历关联 |
| 用户状态           | 业务事务局部设置 app.user_id；停用/注销拒绝业务；连接归池身份复用负例必测                                                                                                                                                |
| Auth 数据          | 登录前查邮箱是受限 Auth 服务操作，不能依赖尚不存在的 app.user_id；Auth 角色只访问 Auth 表，业务角色无密码/Session/验证表访问权                                                                                           |
| Auth 与 users 桥接 | 仅上节窄注册写入；Session 解析后仍检查业务用户状态；auth_users ID 不允许更新                                                                                                                                             |
| 回执/审计/隐私     | 本人范围；其他人 key/resourceId 不可命中；清理系统身份限操作和受控审计                                                                                                                                                   |
| 删除列表/恢复      | 本人专用生命周期查询，默认正文和版本查询仍404；自己的 trash 可识别“已删除”，对别人一律404                                                                                                                                |

不存在、其他账户、软删、硬删的普通资源路径统一404和通用文案；未经授权不查询/返回 currentRevision、标题、存在性。只有已经授权读取到的损坏文档才给 `STORED_RESUME_INVALID`，不返回原正文或 SQL。

Next 私有页面/API 不使用共享 `use cache`/静态生成，RSC 按请求读取 Session。禁止模块级 currentUser 单例。客户端列表使用局部请求状态已足够，无必要不新增 TanStack Query；若已有缓存被复用，key 包含 userId，换账户清空并取消请求。仅 `no-store` 不足以处理浏览器旧内存：换账户销毁 store/协调器、刷新路由；pageshow/bfcache/visibility 恢复时先遮蔽私人内容并重验 Session，再显示。

Cookie 业务写请求：严格可信 Origin+Fetch Metadata+Session 绑定的 CSRF Token；拒绝不支持的媒体类型/跨域请求，明确代理可信头。Auth 保留库保护，不关闭 origin/CSRF 检查；它不会自动保护自定义业务 API。[Better Auth 安全文档](https://better-auth.com/docs/reference/security)

建议限流基线：登录10次/分钟/IP，注册5次/小时/IP，验证/重置邮件每邮箱60秒一次、每小时5次且每IP每小时20次；保存按账户上限60次/分钟、列表120次/分钟、新建/复制20次/分钟。公开前用负载实测确认，不把商业额度当安全限流；不无限锁账号造成拒绝服务。采用持久 PG 计数与入口粗限流，跨进程并发不超预算，429/Retry-After，可信代理剥离伪造IP。库若不能证明必要计数原子性，只补该计数事务，不引入 Redis。[限流文档](https://better-auth.com/docs/concepts/rate-limit)

## 12. IndexedDB、退出与账户切换

不改现有复合键格式，不将旧 fixed 草稿重新标记成真实用户。Session userId 只作为分区标识由受信响应给出，浏览器不能提交它来改变所有权。

| 事件                | 必须执行                                                                                | 验收断言                                                 |
| ------------------- | --------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| 首次装配/刷新       | Session+resume 授权成功后才读取同账户/同简历/tab；验证记录结构                          | B 的 UI/内存不出现 A 的正文                              |
| 401/撤销/过期       | 暂停重试和上传，先落本地；显示未同步、重新登录/救援                                     | 不显示“已保存”；pending envelope 不换 key                |
| 同账号重登          | 重验 userId、资源状态、revision，再恢复原 envelope                                      | 已提交丢 ACK 只取同回执；新远端仍412                     |
| 换 B 登录           | 销毁 A 保存协调器/计时器/请求及 store，清列表缓存；迟到 ACK 只能作用原 A 上下文或被忽略 | 不能将 A 的文档发到 B 的新 resume；不能把 A ACK 当 B ACK |
| 主动退出且已同步    | 全账户/所有标签页先暂停并确认无新写；撤会话、清本账户草稿、全页跳转登录                 | IndexedDB A 分区清空、旧 Session 下一请求401             |
| 主动退出且未同步    | 提供取消退出、在线保存后退出、下载救援并确认清理；显式放弃需二次确认                    | 没有救援/确认不得静默删草稿；等待 ACK 失败仍保留         |
| 网络断开要退出      | 可以先本地锁屏/救援；服务端退出未确认时明确提示，恢复联网前不得宣布彻底退出             | 未撤 Session 的离线退出不能伪称所有设备退出              |
| 其他标签页退出/注销 | BroadcastChannel 只传账户/事件，不传正文/Token；接收端暂停、落盘并屏蔽 UI               | 广播丢失仍由恢复时及写前 Session/owner 检查兜底          |
| 简历软删除          | 暂停所有对应标签页；已同步草稿清理，未同步先救援/隔离，禁止自动回传                     | 旧 pending 不撤销删除；恢复简历后需重新比较基线          |

Cookie 同源共享，不能声称“同一个浏览器两个标签页各登录一个账户”天然成立。测试同时覆盖两个独立 Chrome context 的真实 A/B 与同一 context 中 A→B 的换号竞态。BroadcastChannel 不是安全边界，DB owner/CAS 才是最后保障；发送前重新认证产生的 TOCTOU 也必须由服务器再次授权兜底。

本地存储不是加密保险箱。保留的过期 A 草稿不在 B 界面展示；同源恶意脚本/共享电脑仍可接触磁盘数据，提供账户本地清理及明确提醒。远端设备无法保证立即擦除离线浏览器数据，应在下次在线验证时清除；不向用户作远程彻底擦盘承诺。

## 13. 创建、复制、重命名、删除与恢复规则

创建：服务端生成 resume/version/所有模块条目文本块 UUID，使用现有 Schema 工厂；revision=1、initial version、审计与幂等回执同事务。仅创建请求落库，不因打开 new 页面创建。

复制：来源必须为当前用户当前已保存 revision；锁定/一致性读取来源，若版本变化则412。复制正文、显隐、顺序和受支持样式；重新生成 resumeId、versionId 及全文所有 module/entry/block/link UUID，并重映射 sectionsById、moduleOrder、moduleOverrides。内容相等但 ID 集合不相交。owner 固定当前 Session。**不复制历史版本、草稿、回执、审计、来源绑定和任何分享/派生状态**；只建 revision=1 的初始快照，理由是副本是独立新文档而非历史备份。无已实现头像文件所有权验证时 avatarAssetId 不允许随新用户/复制继承。

重命名：trim 后1—200 Unicode code points，服务端验证；CAS 更新 title 与 updatedAt，revision+1，写 title_snapshot 与原正文快照。编辑器重命名前 flush，成功只更新基线，不用旧 DTO 覆盖正文；并发保存冲突仍走既有412。

建议删除产品政策（待第 23 节确认）：软删立即从普通列表消失、编辑/保存/复制/版本读取404；保留 **7天**，本人在最近删除中恢复，到期后 **24小时内**硬删除；不额外提供“立即永久删除”按钮。时间取数据库，restore_until 在删除时冻结，不能改配置偷偷延长已有期限。恢复在锁定同一行下检查期限、I/R，清 deleted_at/restore_until、revision+1；与清理竞争只有一方成功。恢复不重放旧本地 pending。

软删/恢复/硬删与 deletion_events、审计同事务。软删和恢复均写新生命周期版本，不回退 revision。清理按小批行锁删除 resume_versions/正文及显式资源回执，普通业务不能 UPDATE 历史或执行硬删除。`source_version_id` 自引用和无 FK 的回执/审计必须进入测试，不能以 CASCADE 包含一切作假设。

账户注销：最近重新认证、救援提醒、二次确认后持久化 privacy_request；业务用户同事务 disabled/deleted，下一业务请求立即拒绝。Auth 撤会话失败可重试，但不影响业务禁用已生效；禁止新登录会话获得业务权限。24小时内由受控清理 CLI 删除所有简历/版本、Auth credential/session/验证关联、本地清理通知和可删除标识；无通用 Worker/队列。删除请求重复/崩溃重跑不重复效果。注销后本期不提供恢复账户，未来同邮箱重新注册必须新 UUID。

未来 AI/JD/PDF 表本期不存在，不为其编排；本期采用显式清理对象清单，后续新增持有正文的能力必须修改此清单和恢复负例才可上线。

## 14. 错误状态与可见反馈

| 场景                 | 反馈/后续操作                                             | 禁止                                     |
| -------------------- | --------------------------------------------------------- | ---------------------------------------- |
| Auth 失败/邮件请求   | 通用错误或受理文案；保留邮箱，允许限流后重试              | 账号枚举、Token/内部错误回显             |
| 401                  | 登录失效、未同步草稿保留、同账户重登/救援                 | 自动清草稿或换号继续上传                 |
| 404                  | 无法打开该简历，返回工作台；已在本人 trash 才显示恢复入口 | 对陌生 ID 区分“是别人的”                 |
| 本人损坏/未来 Schema | 暂停编辑上传、保留本地、显示追踪号和联系处理方式          | 空文档覆盖原件、strip 未知字段           |
| 412                  | 原有 base/local/remote 比较；再试仍CAS                    | force overwrite                          |
| 409 幂等键冲突       | 停止自动重试，保留输入                                    | 随机换 key 掩盖逻辑冲突                  |
| 413/422              | 明确超限/字段提示、保留输入/救援                          | 截断正文后报保存成功                     |
| 429                  | Retry-After、限时提示                                     | 逐键重试或永久锁账号                     |
| 列表/CRUD 网络未知   | 保留原列表/输入，原 key 确认结果                          | 请求超时直接认定事务回滚、重复创建       |
| IDB 错误             | 本地备份不可用与云端状态分别显示                          | 本地失败阻断所有在线救援，或伪称本地安全 |
| 删除后其他标签页保存 | 提示资源不可用、允许当前内存救援                          | 自动新建副本/自动恢复删除                |

## 15. UX、响应式与无障碍

视觉沿用 `design.mf` 与原型黑灰分层、细边框、紧凑图标和白色 A4。真实交互优先于原型中的未来按钮。登录沿用原型视觉语言，但注册/验证/找回必须补齐真实状态，不以无图为由省略。

进入 V0.2 前单列三项稳定性：V02-002 容器溢出、003 两侧工具组布局、004 动态 A4 分页。先检查 `444d646` 的实测，不重复整套重构。当前全局 html/body overflow:hidden 会影响新登录/工作台，必须限定编辑器壳层滚动责任；检查 flex/grid 最小尺寸、可用高度与实际内容，不靠全站隐藏 scrollbar。

| 验收尺寸（CSS px，不是截图裁剪尺寸） | 预期                                                                          |
| ------------------------------------ | ----------------------------------------------------------------------------- |
| 1920×1080、1440×900、1366×768        | 编辑器外壳容纳当前视口；正文面板/预览按需内部滚动；工具组间距一致且可完整访问 |
| 1280×720、1024×768                   | 面板按明确断点收起/切换；不能挤小点击目标或将末端按钮推出不可达区域           |
| 1080×1920                            | 高视口工具组按可用空间稳定居中；不是用上下固定大 padding 模拟                 |
| 390×844、200%文本缩放                | 登录/工作台可滚动；编辑器单活动面板与预览切换，保留轻量编辑，不强行塞三栏     |

容器容得下时 rails 不出现无意义纵向/横向滚动；容不下时允许明确的可访问内部滚动，首末按钮键盘可达，目标不因 flex shrink 缩到不可用。工具组采用相同的尺寸/间距规则，不强制数量不同的左右栏逐行对齐。

A4 验收不仅 count 增加：中文/英文/长 URL/emoji/组合字符、极长段落、多个连续模块、字体/边距/缩放、增删内容后分页增减；逐页内容盒不溢出，文本片段串接与原文相等、无重复/缺字，标题不孤立，末页和页间距可滚动到达。测量面和页内字体/行高/宽度一致；当前 CSS 两者排版上下文不同，列为待验证风险，不将它写成已复现根因。

全流程 Tab/Enter/Space、Esc、焦点回退、图标可访问名称、错误关联字段、保存/复制/删除 aria-live；对话框焦点不逃逸，删卡片后焦点落到邻卡/创建入口。Chrome+NVDA 人工读屏记录；自动无障碍检查不能替代读屏。登录表单支持密码管理器及正确 autocomplete，不禁止粘贴密码。

## 16. 性能预算

不放宽 `platform/performance-budget.json`：输入→预览 p95≤100ms（≥50样本）、IDB提交 p95≤400ms（≥50）、分页 p95≤500ms、保存 API p95≤500ms/p99≤1000ms（≥300）、非预期失败率<0.1%、参考 RTT≤100ms 时输入→ACK p95≤2000ms（≥50）。Node 纯分页基准不替代 DOM 测量与完整页面分页延迟。

工作台新增建议预算：100份合成简历的分页元数据查询 p95≤300ms、同区域新建/重命名/复制/软删 p95≤500ms，至少300次样本；Chrome 列表首屏在固定参考环境 p95≤1500ms（50次），失败样本单列，不剔除变绿。名称、时间、动作的元数据 DTO 不携带正文；不做全文索引或图片缩略图生成。

Auth 不以降低密码哈希成本换速度；登录处理和真实邮件送达分开测量。性能报告含硬件、版本、并发、网络、p50/p95/p99、错误/跳过数量。数据库连接池须汇总 Auth+业务+清理预算；测不到只能标未验证，不降低阈值。

## 17. 安全与隐私

不自研密码哈希、验证/重置协议。最小化库端点，只启用邮箱密码；禁止前端 ownerId、会话 Token 或数据库/邮件密钥。所有生产密钥由受控配置注入，`.env.local`、node_modules、构建物、真实 dump 不提交。

输入 strict schema、1MiB 请求上限、输出转义、链接白名单；受控代理请求体大小限制与应用一致，防止先无界读完 body 再拒绝。CSP/安全响应头与当前 Next 构建验证，不以允许任意域修兼容问题。

日志字段白名单：时间、traceId、操作、结果码、耗时、最少不透明资源标识；不记录密码、Cookie、Token、URL验证参数、SQL bind、完整简历正文、邮件内容。公共 CI 连合成凭据/Token 也不上传；认证测试默认禁用原始 trace/HAR/video，输出脱敏断言摘要。例外受控现场排障不得提交仓库。

建议保留：在线未删简历及版本随资源生命周期；邮件验证24小时/重置30分钟，过期记录24小时内清理；回执≥7天，过期未确认保存禁止换 key 盲重放；Auth/IP限流明细最短必要窗口，安全审计建议90天但须按实际适用法律审查；正文备份建议30天滚动，删除事件至少保留到所有可能复活正文的备份退出后再加7天。这里只是工程建议，不是法定期限。备份/审计期限须批准，不默认永久保留。

用户对本机导出的 JSON 自行保管，界面提示含个人信息；服务器不留一份所谓救援备份。账户删除与简历回收站不同：注销不得强制等待7天恢复期。无法立即物理擦除的备份须限制恢复用途、到期退出、恢复前重放删除，不谎称即时清除所有介质。

业务写事务与注销按固定顺序锁用户、再锁简历：写操作持有用户共享行锁并检查 active，注销更新用户状态需排他锁，使已开始的合法写入先结束、停用提交后的新写入必拒绝。退出不能撤销此前已提交的保存；迟到响应仍只属于原账户，不得用于新账户状态。测试必须按这个提交边界判定，不假定取消 fetch 等于撤回数据库事务。

## 18. 中国大陆公开发布外部阻断项

本节是发布核查清单，不替代法律意见。数据处理告知、最短必要保留与删除等需按实际主体/处理情形审查；不能用“只做简历”推定豁免。[个人信息保护法原文](https://www.cac.gov.cn/2021-08/20/c_1631050028355286.htm)

| 阻断项               | 必需证据                                                    | 责任/不能替代的东西                            |
| -------------------- | ----------------------------------------------------------- | ---------------------------------------------- |
| 运营主体、地域与域名 | 生产主体、境内部署/PG区域、域名/TLS、适用备案及运营条件确认 | 用户/运营；虚拟机或开发 localhost 不是生产资格 |
| 邮件                 | 选定供应商、发件域验证、真实验证/重置送达与退信限流         | 用户/运维；Fake/SMTP成功返回不等于收件箱可用   |
| 政策和注销           | 隐私政策/协议/注销文案批准、处理期限、联系人、数据流图      | 用户/合规；独立复核缺失须如实记录              |
| 生产凭据             | Auth/PG/邮件密钥权限、轮换及旧密钥失效演练                  | 运维；不在 CI/文档保存值                       |
| 备份与 PITR          | 同地域加密备份、WAL连续性、隔离恢复实测                     | 运维；pg_dump/pg_restore 不能代替 PITR         |
| RPO/RTO              | 建议≤5分钟/≤60分钟，经采购能力和演练批准                    | 用户/运维；当前无已达标声明                    |
| staging              | 同生产构建 digest、Auth/HTTPS/迁移/回滚/恢复/真实邮件证据   | 开发/验收；本地绿色不替代                      |
| 值班与监控           | 保存/Auth/邮件/清理/备份告警可送达、主备责任、运行手册      | 个人开发不虚构多人审批/7×24能力                |

邮件、遥测、备份也属于数据路径，不默认境外传输。具体域名/备案和其他运营条件须由运营按实际服务/接入方核实；本期无 AI，不提前建设 AI 合规功能或买模型服务。

## 19. 测试策略与验证矩阵

### 19.1 当前真实命令

所有 `pnpm` 命令在 `E:\Resume-opt\platform` 执行；以下脚本已在 package.json 存在。本轮文档审计未运行它们，表内是实施任务的验证要求。数据库命令只面向隔离测试资源；现有迁移测试会在开发数据库建随机 schema，**不是只读命令**，执行前确认独立目标，不指向有真实数据的生产库。

| 代号 | 可运行命令                                                                                                                                 | 前置/结果边界                                                              |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
| F    | `pnpm ci:fast`                                                                                                                             | 类型生成、格式/Lint/类型/依赖边界/Schema/单测；新增组件测试沿 tests/unit   |
| I    | `pnpm test:integration`                                                                                                                    | 本地受保护 env/CI临时 PG；新增Auth/CRUD/RLS测试放此目录                    |
| M    | `pnpm test:migrations`                                                                                                                     | 空库/0001旧库/重复/失败回滚测试扩此入口                                    |
| S    | `pnpm test:save`                                                                                                                           | 既有保存可靠性实验，不能删去                                               |
| B    | `pnpm test:browser`                                                                                                                        | 已安装原生 Chrome+可用测试Web/PG；V02-032 扩配置为 fixed/auth 两个明确项目 |
| P    | `pnpm perf:baseline -- --environment ci --output artifacts/ci/performance.json`                                                            | 现有纯Renderer基准；其他浏览器/API预算另由 B/I 的新增用例产出              |
| R    | `pnpm ops:restore:rehearse -- --output artifacts/ci/restore.json`                                                                          | 现有隔离逻辑恢复，脚本仅允许测试环境                                       |
| N    | `pnpm ops:restore:native -- --output artifacts/ci/native-restore.json`                                                                     | 本机须有匹配 pg_dump/pg_restore；CI使用已有独立PostgreSQL容器配置          |
| G    | `pnpm build`                                                                                                                               | 生产构建及路由/客户端依赖审计                                              |
| ALL  | `pnpm ci:full`，再运行 `pnpm test:migrations`、`pnpm test:browser`、`pnpm ops:restore:native -- --output artifacts/ci/native-restore.json` | 当前 ci:full **不单独包含**Chrome/原生恢复；不能只跑它就报完整通过         |
| D    | 根目录 `git diff --check`；`git status --short`                                                                                            | 文档/范围检查，不触库                                                      |

任务中的代号均引用上表精确命令，不代表新增同名脚本。需要的清理 CLI/发布流程是任务产物；不提前编造 `pnpm ops:cleanup`、`test:a11y`、`release:verify` 等不存在命令。手工 staging 操作在选择平台后编写并审查精确命令，再执行；此前以阻断项记录。

### 19.2 必须执行的矩阵

| ID/层级           | 场景与断言                                                                              | 环境/证据                                  | 失败等级   |
| ----------------- | --------------------------------------------------------------------------------------- | ------------------------------------------ | ---------- |
| T01 Auth 合同     | 注册/登录/退出/Session/未验证/重复邮箱、字段边界及安全DTO                               | 单测+真实PG+handler HTTP，Fake邮件         | P1；泄露P0 |
| T02 Session       | 过期、重置全部撤销、退出、停用、旧Cookie及并发写                                        | 真PG+Chrome；只记录状态不存Cookie          | P0/P1      |
| T03 Token/邮件    | 过期±1秒、重复消费、并发消费、篡改、错误用途、重发/投递失败                             | Fake可测合同；staging真实邮箱确认送达/链接 | P1         |
| T04 枚举/限流     | 存在/不存在响应一致，混合并发/IP伪造/多进程计数/429                                     | 真PG/HTTP；脱敏状态与时间统计              | P1         |
| T05 Cookie/CSRF   | HttpOnly/Secure/SameSite/host-only；跨源/缺令牌/伪Origin/开放跳转拒绝；Token不进JSON/JS | 真Chrome HTTPS测试及HTTP负例               | P0/P1      |
| T06 双账户 API    | A调用B的GET/save/list/rename/copy/delete/undelete/回执，统一不可见无写入                | 两个真实注册账号，非 fixed mock            | P0         |
| T07 RLS/角色      | 无身份、A/B连接池复用、写换owner、跨owner版本FK、Auth角色读正文/业务角色读Token均失败   | 真PG实际非owner角色；SQL断言               | P0         |
| T08 CRUD          | 同key重复/异body、并发CAS、版本插入失败全回滚、copy ID重映射和来源变化                  | 真PG；数量/哈希/ID集合，不含正文           | P0/P1      |
| T09 删除          | 软删404、期限边界、恢复与purge竞争、旧pending/旧回执、注销重试                          | 真PG+Chrome；生命周期事件序号              | P0/P1      |
| T10 草稿          | 原生IDB分区、刷新、未同步退出、quota、A→B迟到ACK/网络恢复/多标签                        | 原生Chrome；fake-indexeddb仅快测           | P0         |
| T11 动态路由/缓存 | 两份本人简历切换，A/B相同URL、RSC/预取/后退/bfcache、服务器重启                         | production build Chrome+HTTP               | P0/P1      |
| T12 编辑正确性    | IME、条目/模块增删复制排序显隐、412、丢ACK、最终刷新                                    | 既有单测/真PG/Chrome，保留旧测试           | P0/P1      |
| T13 视觉/A4       | 第15节全部尺寸，动态增减页、文本完整、页边界、无多余外滚动和不可达控件                  | Chrome图像/几何断言；只用合成简历          | P1         |
| T14 无障碍        | 登录→创建→保存→复制→删除恢复→退出纯键盘；200%/读屏                                      | 自动检测+NVDA人工记录姓名日期版本          | P1         |
| T15 迁移/回滚     | 空库、0001有数据、重跑、坏校验和、故障中断、应用兼容                                    | 真PG；hash/revision/角色前后比较           | P0/P1      |
| T16 恢复/删除重放 | 逻辑+原生恢复；删除发生于备份后，重放后不可见；恢复事件不被误删；所有旧Session撤销      | 真PG；独立删除清单水位                     | P0/P1      |
| T17 质量/性能     | F/G/P和第16节所有实测预算、边界脚本负例                                                 | 本地+CI固定环境、样本数/首次失败记录       | P1         |
| T18 发布运维      | staging真实邮件/TLS/告警/迁移回滚/密钥轮换/PITR                                         | 外部平台人工及机器证据                     | B阶段阻断  |

P0：跨账户泄露/写入、凭据泄露、已ACK正文丢失、删除复活；P1：核心流程不能完成、恢复/迁移失败、邮件不可用、CSRF/限流缺失、内容裁切/不可达、预算不达、必要证据缺失。候选版和公开版均不得带已知 P0/P1；外部条件未满足不能用“仅一个小问题”豁免 B。

## 20. 数据迁移、备份、恢复与回滚

先 expand，不改已执行 0001；在隔离库加载包含两 fixed users、多个 revision/回执/已删简历的合成旧库，升级后比较正文 canonical hash、owner、revision 和历史行数。旧 fixed 内容不被转换成可登录账号。

迁移复跑必须 no-op；校验和变化失败；DDL或注册桥接失败整个事务回滚；新表权限检查失败停止发布。现有迁移器把本批 up.sql 放单事务，所以不在其中加入 `CREATE INDEX CONCURRENTLY`。先按本期小表常规事务建索引，真实测到锁预算问题再单独设计非事务路径。

应用回滚只允许退到**已经具备真实Auth和当前数据库兼容性**的前一 V0.2 构建；公开环境绝不能退回 fixed-only `844f4ee`。首次真实Auth发版没有安全旧版时关闭入口/维护并 forward-fix。down 仅用于空测试库证明可逆，不丢生产用户数据换回滚成功。

恢复顺序：隔离新目标 → 禁邮件和用户流量 → 还原备份/迁移 → 导入最新独立删除清单 → 按事件序号重放软删/恢复/硬删/注销 → 撤销全部旧Session和验证/重置Token → 角色/RLS/哈希/保存核验 → 记录缺失窗口 → 人工批准 → 只保留单一写入集群。

删除清单不能只放在将被回滚的数据库内。最小方案：业务 deletion_events 同事务写入；受控运维定时导出到与业务恢复目标独立的加密持久卷/备份通道（不建设对象存储服务），按序号校验连续性与已导出水位。清理前必须确认相应事件已进入独立通道。事故时若备份后到事故点的删除事件无法完整取得，**保持恢复库隔离，不准上线**；定时导出不能保证零窗口，须在 B 阶段根据已批准 RPO 实测或采用已有备份平台的连续同步能力，不能静默忽略尾部删除。

软删后用户可能恢复，故不能把历史所有 delete 永久再删一遍；按资源最后有效事件及硬删/注销终态处理。旧备份缺少删除清单水位、密钥或完整性校验均阻断。备份30天退出、删除事件额外7天是建议策略；生产 PITR、加密和跨主机 RPO/RTO 尚未验证。

## 21. GitHub Actions 与发布证据

保留临时 PG、固定版本、最小权限、失败非零退出和原生恢复 `if: always()`。补 fixed 回归与真实 Auth 两个隔离测试配置；真实 Auth 测试现场注册随机 UUID 用户，不能依赖生产固定 UUID。现有 Playwright config 把 webServer env 写死 fixed，必须改成明确项目启动隔离，防止“Auth E2E 实际还在测 fixed”。不复用不明身份模式的3000端口服务。

新增矩阵、必需报告清单和失败注释；保留首次失败/重试记录，flaky 不自动算发布通过。证据缺失必须失败：现有 upload 的 `if-no-files-found: warn` 不足以作为 V0.2 发布证明。迁移/Chrome失败后仍运行可执行的恢复和脱敏摘要，不能 continue-on-error 把总门禁变绿。

| 证据                                                          | 保留与访问                                            |
| ------------------------------------------------------------- | ----------------------------------------------------- |
| CI机器摘要、JUnit、预算、角色/RLS/迁移/恢复断言、合成视觉截图 | 建议CI保留30天；当前14天仅为旧配置；无密码/Token/正文 |
| 人工验收、变更/回滚记录、release manifest及清单hash           | 建议受控保留90天；与隐私策略一致                      |
| 原始Auth trace/HAR、数据库dump、真实邮箱/凭据、生产正文       | 禁止公共CI上传；默认不采集Auth trace，失败也不能泄露  |

每份证据绑定 commit SHA、构建digest、schema/migration版本、Auth配置版本、环境、runId、时间、测试/人工执行人；缺少人手不虚构签字。staging同digest晋级，不能生产重建后套用旧报告。本地提交采用规定类型，用户手动推送；CI状态不由本地文档手工改成成功。

## 22. 风险登记

| 风险/依据                                    | 影响                                   | 对应关闭任务  |
| -------------------------------------------- | -------------------------------------- | ------------- |
| `444d646`仅本地，窗口尺寸/长段落测试覆盖有限 | 新路由可能被全局禁滚动、A4测量差异影响 | 002—004、029  |
| 假分享/PDF/会员入口仍存在                    | 用户误判能力，违反范围                 | 005           |
| 固定身份端口没有请求上下文，真实库未装配     | 误复用全局身份/配置落回开发            | 006—009、013  |
| Auth建用户与业务用户不原子                   | 孤立账号、重复身份、错误认领固定数据   | 008、027      |
| Session切换与旧协调器/ACK竞争                | A草稿污染B或清掉更新输入               | 015—016、028  |
| RLS只有owner，没有完整生命周期约束           | 删除/停用后仍能通过漏查路径读取        | 013、021、024 |
| 回执/审计无外键、版本自引用                  | 硬删不完整/清理失败                    | 024—027       |
| 备份后删除日志缺尾/恢复事件被忽略            | 已删除正文复活或误删已恢复资源         | 026、033      |
| 文档写有历史API但正式路由没有                | 计划错误估计可复用工作量               | 001、006      |
| 邮件与主体/域名/生产PG未决定                 | 可完成代码但无法公开注册运营           | 010、033、035 |
| 根文档目标与代码约束/脚本不一致              | 误放宽保存限制或假跑门禁               | 001、030、032 |

## 23. 尚需确认的决策

仅列会改变实现或发布条件的事项：

1. 是否批准本稿最小范围（不做搜索/历史浏览/PDF分享；只用现有基础模板）以及**邮箱验证完成后才允许私有业务**？建议批准；后者比旧 §14.5 更严格，会改变注册进入流程。
2. 是否批准**简历7天可恢复、到期24小时内清理；账户注销不可恢复且24小时内清理**？如要求立即不可恢复，可删回收站/undelete任务，但须重新确认产品文案与恢复策略。
3. 是否批准保留策略与运维目标：备份建议30天、删除清单覆盖全部备份生命周期+7天、安全审计/证据期限分开、RPO≤5分钟/RTO≤60分钟？批准前只作合成测试参数，不写成法律承诺。
4. 实际发布由哪个主体、境内区域/PG、域名及邮件发件服务承载？何人负责政策审核与发布验收？无需现在凭空指定供应商；这些未定阻断公开发布，不阻断合同和本地合成开发。

共享UUID/独立Auth表、Session/限流建议参数属于技术推荐，不要求用户替工程师选择库API；V02-006 若验证不成立须拿具体失败与最小替代方案回审，不偷偷换架构。

## 24. 里程碑与依赖

| 里程碑                     | 进入条件                         | 任务          | 退出条件                                              |
| -------------------------- | -------------------------------- | ------------- | ----------------------------------------------------- |
| M0 进入V0.2前界面稳定性    | 只读基线核实                     | 002—005       | 当前SHA Chrome布局/A4无阻断，伪入口移除，用户确认视觉 |
| M1 路线/身份合同冻结       | 用户批准本稿；M0代码可先独立验收 | 001、006、007 | 路线同步、身份映射/实际Auth合同/配置边界冻结          |
| M2 Better Auth/真实Session | M1退出                           | 008—012       | 真PG注册登录验证重置退出、Token/CSRF/枚举/限流通过    |
| M3 隔离与动态编辑入口      | M0、M2退出                       | 013—014       | 双账户数据库/API隔离和动态路由通过；仍只受控测试      |
| M4 CRUD与工作台            | M3退出                           | 017—023       | 新建/列表/重命名/复制/删除/恢复闭环；整体退出还依赖M5 |
| M5 草稿/退出/换号          | M3退出，可与M4非重叠工作顺序交替 | 015—016       | 原生IDB换号和退出负例通过；工作台发布不得跳过         |
| M6 删除/隐私/恢复          | M4、M5退出                       | 024—027       | 硬删/注销/删除清单重放、旧库迁移回滚通过              |
| M7 候选版与发布验证        | M0—M6退出                        | 028—035       | 034关闭A候选版；033与035关闭B公开就绪，不能混为一个勾 |

实现可以按依赖拓扑顺序逐项提交；不建议单人同时开启多套未验证大改。核心链：001→006→007/008→009→010/012→013→014→015→016；CRUD从014分出，最后在022/028汇合。具体完整依赖见任务表。

## 25. 逐项任务分解

所有任务默认不做第5节非范围。路径缩写 `web`=`platform/apps/web/src`，`app`=`platform/packages/application/src`，`infra`=`platform/packages/infrastructure/src`，`domain`=`platform/packages/domain/src`，`tests`=`platform/tests`。验证代号展开见19.1，均为现有可运行命令。每项代码任务要先加入能失败的回归再修复；任务完成后 F 必跑，影响构建再 G，涉及PG/浏览器不得用mock代替。每项独立本地提交，提交信息用 feat/fix/docs/style/refactor/test/chore；绝不自动push。

### 25.1 完整任务/依赖索引

| ID      | 单一交付目标                 | 前置依赖                     |
| ------- | ---------------------------- | ---------------------------- |
| V02-001 | 批准后同步版本路线与范围合同 | 用户批准本稿                 |
| V02-002 | 编辑器视口/滚动责任稳定      | 基线444d646核验              |
| V02-003 | 左右工具组可用高度布局稳定   | 002                          |
| V02-004 | 动态A4分页完整性稳定         | 002                          |
| V02-005 | 移除正式页未来/模拟入口      | 001                          |
| V02-006 | 冻结身份与Auth适配合同       | 001                          |
| V02-007 | 分离真实环境与固定测试配置   | 006                          |
| V02-008 | Auth表/共享身份迁移          | 006、007                     |
| V02-009 | 注册登录与PG Session服务     | 008                          |
| V02-010 | 邮箱验证/密码重置生命周期    | 009                          |
| V02-011 | 认证页面真实状态闭环         | 010、002                     |
| V02-012 | Cookie/CSRF/防枚举/限流边界  | 009、010                     |
| V02-013 | 真实身份业务RLS隔离          | 009、012                     |
| V02-014 | 动态resumeId编辑入口         | 003、004、005、011、013      |
| V02-015 | Session过期的草稿保全与恢复  | 014                          |
| V02-016 | 退出与跨账户切换屏障         | 015                          |
| V02-017 | 创建空白/基础模板简历事务    | 013                          |
| V02-018 | 本人元数据分页列表           | 013                          |
| V02-019 | 重命名CAS事务                | 014                          |
| V02-020 | 独立副本事务及ID重映射       | 017                          |
| V02-021 | 软删除/期限恢复生命周期      | 019                          |
| V02-022 | 最小工作台与创建页           | 016、017、018、019、020、021 |
| V02-023 | 最近删除与恢复UI             | 021、022                     |
| V02-024 | 有界硬删除清理               | 021                          |
| V02-025 | 账户注销与停用闭环           | 010、016、024                |
| V02-026 | 恢复时删除清单重放           | 024、025                     |
| V02-027 | V0.1升级与安全回滚演练       | 008、021、024、025、026      |
| V02-028 | 双账户Chrome全链E2E          | 022、023、025、027           |
| V02-029 | 视觉/键盘/读屏验收           | 028                          |
| V02-030 | 真实Session性能预算验收      | 028                          |
| V02-031 | 脱敏、安全配置与运行告警     | 012、024、025                |
| V02-032 | V0.2 CI及证据完整性门禁      | 027、028、029、030、031      |
| V02-033 | staging真实依赖与发布演练    | 032、外部配置到位            |
| V02-034 | A：代码候选版验收            | 032                          |
| V02-035 | B：中国大陆公开发布就绪决策  | 033、034、外部阻断全部关闭   |

### V02-001

| 字段           | 要求                                                                     |
| -------------- | ------------------------------------------------------------------------ |
| ID             | V02-001                                                                  |
| 任务           | 批准后同步路线合同，docs提交                                             |
| 前置依赖       | 用户批准本稿及第23节实现相关决定                                         |
| 代码影响范围   | 根产品/技术/UX/README/V0.1和本计划；无业务代码                           |
| 数据库影响     | 无                                                                       |
| API/页面影响   | 文档清单冻结，不实现端点                                                 |
| 安全不变量     | 历史CI不套用新SHA；不删历史证据                                          |
| 必须新增的测试 | 文档链接/任务依赖/禁入范围一致性检查                                     |
| 验证命令       | D；`node scripts/validate-technical-selection.mjs`（根目录已有结构检查） |
| 完成证据       | 批准记录、冲突条目逐项关闭、文档diff                                     |
| 回滚方式       | revert本次文档提交，不回滚业务数据                                       |
| 完成标准       | 不再存在两个有效V0.2；待排PDF无擅定版本                                  |
| 明确不做       | 本轮草案不提前执行本任务；不启用AI赠额                                   |

### V02-002

| 字段           | 要求                                                                   |
| -------------- | ---------------------------------------------------------------------- |
| ID             | V02-002                                                                |
| 任务           | 修复/确认编辑器视口与滚动归属，fix或test提交                           |
| 前置依赖       | 审计444d646；本任务与Auth分离                                          |
| 代码影响范围   | web/app/globals.css、client/editor CSS、tests/browser                  |
| 数据库影响     | 无                                                                     |
| API/页面影响   | 编辑器壳层；新普通页面不得继承禁滚动                                   |
| 安全不变量     | 不能隐藏不可达内容；不触保存协议                                       |
| 必须新增的测试 | 第15节尺寸下document/client/scroll几何、面板末字段可达；长普通页可滚动 |
| 验证命令       | F、B、G                                                                |
| 完成证据       | 各尺寸截图/几何JSON、当前SHA CI                                        |
| 回滚方式       | 仅撤CSS与对应UI改动，保留故障用例                                      |
| 完成标准       | 可容纳时无外层溢出；内容长时正确面板可滚                               |
| 明确不做       | 不只隐藏scrollbar，不全局固定业务页高度                                |

### V02-003

| 字段           | 要求                                                |
| -------------- | --------------------------------------------------- |
| ID             | V02-003                                             |
| 任务           | 两侧工具组稳定布局，fix/style提交                   |
| 前置依赖       | V02-002                                             |
| 代码影响范围   | web/client/editor CSS/icons，tests/browser          |
| 数据库影响     | 无                                                  |
| API/页面影响   | 两侧图标栏                                          |
| 安全不变量     | 所有功能可键盘访问；不更改编辑动作                  |
| 必须新增的测试 | 高/短视口组中心、等距、点击目标不缩小、首末按钮可达 |
| 验证命令       | F、B、G                                             |
| 完成证据       | 三类高度截图与按钮边界断言                          |
| 回滚方式       | 撤工具栏布局提交                                    |
| 完成标准       | 无大固定padding模拟居中，足够高时不无意义滚动       |
| 明确不做       | 不强制左右不同数量图标逐行对齐                      |

### V02-004

| 字段           | 要求                                                                |
| -------------- | ------------------------------------------------------------------- |
| ID             | V02-004                                                             |
| 任务           | 验证并补齐动态A4完整分页，fix/test提交                              |
| 前置依赖       | V02-002                                                             |
| 代码影响范围   | packages/resume-renderer、web/client/editor测量、tests/unit/browser |
| 数据库影响     | 无                                                                  |
| API/页面影响   | 实时预览及页数                                                      |
| 安全不变量     | 原文/ID不变、无裁切、保存协议不变                                   |
| 必须新增的测试 | 测量与渲染字体一致；全文串接相等、页边界、末页、增页减页、组合字符  |
| 验证命令       | F、P、B、G                                                          |
| 完成证据       | 合成样本、几何/文字断言、p95和截图                                  |
| 回滚方式       | 撤Renderer/测量变更；仍有裁切则保持版本阻断                         |
| 完成标准       | 全部内容有且仅有一次、按内容增减页、原预算通过                      |
| 明确不做       | 不开发PDF/新模板引擎，不用截图或overflow遮缺字                      |

### V02-005

| 字段           | 要求                                                     |
| -------------- | -------------------------------------------------------- |
| ID             | V02-005                                                  |
| 任务           | 清除正式页伪功能入口，fix提交                            |
| 前置依赖       | V02-001                                                  |
| 代码影响范围   | web/client/editor/editor-workspace.tsx及直接相关CSS/测试 |
| 数据库影响     | 无                                                       |
| API/页面影响   | 分享/PDF/AI/JD/会员假文案及空动作移除                    |
| 安全不变量     | JSON救援、真实样式控件和正文保留                         |
| 必须新增的测试 | 未来入口/示例分享链接/次数不存在，救援和真实工具仍可用   |
| 验证命令       | F、B、G                                                  |
| 完成证据       | 删除清单与正常编辑截图                                   |
| 回滚方式       | 不公开重新引入假能力；必要时回退整个受控候选版           |
| 完成标准       | 所有可见按钮均有本期真实动作，无误导权益                 |
| 明确不做       | 不把假入口实现成新功能，不清理无关代码                   |

### V02-006

| 字段           | 要求                                                                    |
| -------------- | ----------------------------------------------------------------------- |
| ID             | V02-006                                                                 |
| 任务           | 冻结Better Auth版本、共享身份和handler合同，test/docs提交               |
| 前置依赖       | V02-001                                                                 |
| 代码影响范围   | infra/identity、app/ports、tests/unit/integration、platform/docs        |
| 数据库影响     | 仅隔离验证生成DDL和角色设计；正式迁移在008                              |
| API/页面影响   | 实际Auth允许端点、安全Session DTO、错误矩阵冻结                         |
| 安全不变量     | 无自研密码、无可登录固定数据、无Token进JS                               |
| 必须新增的测试 | 锁定库的合同快照、UUID/字段映射、Session响应Token扫描、注册桥接事务失败 |
| 验证命令       | F、I、G；实施时锁版本依赖另行提交，不用latest                           |
| 完成证据       | 精确版本、生成DDL diff、合同与桥接选择ADR                               |
| 回滚方式       | 移除尚未接业务的验证接入；不改旧表                                      |
| 完成标准       | 第9/10节假设有可运行证据，不成立项回审                                  |
| 明确不做       | 不建设通用身份平台/OAuth/映射框架                                       |

### V02-007

| 字段           | 要求                                                                 |
| -------------- | -------------------------------------------------------------------- |
| ID             | V02-007                                                              |
| 任务           | 分离环境配置与运行权限，refactor/fix提交                             |
| 前置依赖       | V02-006                                                              |
| 代码影响范围   | packages/config、infra/postgres、web/server/runtime、scripts配置入口 |
| 数据库影响     | 新角色连接配置；不搬迁数据库                                         |
| API/页面影响   | 启动/readiness与开发bootstrap                                        |
| 安全不变量     | staging/prod fixed立即失败；运行进程不要求迁移全权凭据               |
| 必须新增的测试 | 四环境×两Auth模式、缺配置、开发seed误指生产、配置异常不回显URL       |
| 验证命令       | F、I、G                                                              |
| 完成证据       | 启动矩阵与客户端依赖边界结果                                         |
| 回滚方式       | 回退配置代码仅限受控测试；公开模式不得退fixed                        |
| 完成标准       | 业务/Auth/迁移/测试凭据分离且错误环境fail-closed                     |
| 明确不做       | 不搭配置中心、不读取输出本机密码                                     |

### V02-008

| 字段           | 要求                                                                   |
| -------------- | ---------------------------------------------------------------------- |
| ID             | V02-008                                                                |
| 任务           | 落地Auth与业务共享UUID迁移，feat提交                                   |
| 前置依赖       | V02-006、V02-007                                                       |
| 代码影响范围   | platform/database/migrations、infra/database、tests/integration        |
| 数据库影响     | Auth标准表/索引/约束、最小权限角色、窄注册桥接                         |
| API/页面影响   | 为Auth准备持久存储，无工作台                                           |
| 安全不变量     | 不改存量owner/0001；Auth角色不能读正文                                 |
| 必须新增的测试 | 新旧库迁移、同ID唯一、桥接回滚、重复邮箱、固定用户不能被认领、权限负例 |
| 验证命令       | F、M、I、R                                                             |
| 完成证据       | DDL审查、行数/hash与权限矩阵                                           |
| 回滚方式       | 测试空表可down；有真实账户只停入口/forward-fix                         |
| 完成标准       | 每个Auth用户唯一业务用户，旧简历零变化                                 |
| 明确不做       | 不建AI/JD/订单等表，不把触发器变通用框架                               |

### V02-009

| 字段           | 要求                                                      |
| -------------- | --------------------------------------------------------- |
| ID             | V02-009                                                   |
| 任务           | 注册/登录/退出与PG Session服务，feat提交                  |
| 前置依赖       | V02-008                                                   |
| 代码影响范围   | infra/identity、web/server、web/app/api/auth、app身份端口 |
| 数据库影响     | 使用Auth表；无新业务表                                    |
| API/页面影响   | Auth handler和安全session读取接口                         |
| 安全不变量     | 请求级身份、库哈希、Session不外露、无客户端owner          |
| 必须新增的测试 | T01/T02、会话撤销下一请求失败、请求A/B并发不串身份        |
| 验证命令       | F、I、G                                                   |
| 完成证据       | 真实PG handler报告与安全DTO断言                           |
| 回滚方式       | 停真实入口/撤新Session，保留数据，回兼容Auth构建          |
| 完成标准       | Session生命周期真PG通过，无固定模式fallback               |
| 明确不做       | 不注册即发额度、不做设备管理/第三方登录                   |

### V02-010

| 字段           | 要求                                                                |
| -------------- | ------------------------------------------------------------------- |
| ID             | V02-010                                                             |
| 任务           | 邮箱验证与重置密码生命周期，feat提交                                |
| 前置依赖       | V02-009                                                             |
| 代码影响范围   | infra Auth/mail、web/server认证装配、tests                          |
| 数据库影响     | 使用标准verification/account/session，无邮件队列                    |
| API/页面影响   | 库验证/找回/重置端点与回调URL                                       |
| 安全不变量     | 一次性Token、重置撤旧Session、通用受理响应                          |
| 必须新增的测试 | T03/T04：时间边界/并发消费/重复重置/发信失败/旧Session与旧Token失效 |
| 验证命令       | F、I、G                                                             |
| 完成证据       | Fake邮件合同报告；真实邮件留033单独证明                             |
| 回滚方式       | 停受影响发送、保留账号，撤未消费Token后兼容回退                     |
| 完成标准       | 合同全通过；缺真实邮件供应商明确阻断公开化                          |
| 明确不做       | 不在本任务默认采购供应商、不做短信/Worker                           |

### V02-011

| 字段           | 要求                                                                     |
| -------------- | ------------------------------------------------------------------------ |
| ID             | V02-011                                                                  |
| 任务           | 认证页面可完成真实流程，feat提交                                         |
| 前置依赖       | V02-010、V02-002                                                         |
| 代码影响范围   | web/app认证页面、client/auth、tests/unit/browser                         |
| 数据库影响     | 无DDL                                                                    |
| API/页面影响   | login/register/verify/forgot/reset，登录后站内返回                       |
| 安全不变量     | 密码不持久化/回显；失败不暴露账户存在性                                  |
| 必须新增的测试 | 成功/失败/等待/过期、Enter/Tab、密码管理器属性、外站returnTo拒绝、短视口 |
| 验证命令       | F、B、G                                                                  |
| 完成证据       | 合成账户流程截图/键盘记录                                                |
| 回滚方式       | 回兼容页面，Auth数据不回滚                                               |
| 完成标准       | 不依赖控制台操作能注册验证登录找回                                       |
| 明确不做       | 不添加营销页、社交按钮或原型模拟成功                                     |

### V02-012

| 字段           | 要求                                                                  |
| -------------- | --------------------------------------------------------------------- |
| ID             | V02-012                                                               |
| 任务           | 关闭Cookie认证的请求伪造/滥用入口，fix/feat提交                       |
| 前置依赖       | V02-009、V02-010                                                      |
| 代码影响范围   | web/server/http、infra Auth限流、配置、tests                          |
| 数据库影响     | PG持久限流最小表/索引；必要Session绑定CSRF字段                        |
| API/页面影响   | 全部Cookie写接口、csrf读取、429                                       |
| 安全不变量     | 不关闭库CSRF；业务与Auth保护均独立成立                                |
| 必须新增的测试 | T04/T05、跨域简单请求/无Origin政策、伪代理IP、并发计数、Token不进日志 |
| 验证命令       | F、I、B、G                                                            |
| 完成证据       | HTTP/Chrome HTTPS安全断言，不上传原始Token                            |
| 回滚方式       | 关闭不安全写入口；不通过撤保护恢复服务                                |
| 完成标准       | 所有启用写路径矩阵通过，多进程限流有效                                |
| 明确不做       | 不添加Redis/验证码服务/通用WAF平台                                    |

### V02-013

| 字段           | 要求                                                    |
| -------------- | ------------------------------------------------------- |
| ID             | V02-013                                                 |
| 任务           | 真实Session下业务隔离，fix/feat提交                     |
| 前置依赖       | V02-009、V02-012                                        |
| 代码影响范围   | app用例、infra/resume事务与RLS迁移、web/server          |
| 数据库影响     | active用户/资源生命周期策略、版本父资源过滤、角色授权   |
| API/页面影响   | 现有读取/保存，所有新用例复用同一授权入口               |
| 安全不变量     | SET LOCAL同连接；无owner/BYPASSRLS；无他人revision泄露  |
| 必须新增的测试 | T06/T07，真实注册A/B、缺身份/连接池复用、停用与版本越权 |
| 验证命令       | F、I、M、S                                              |
| 完成证据       | 实际数据库角色矩阵与API负例                             |
| 回滚方式       | 先阻断公开流量；保留RLS，forward-fix错误策略            |
| 完成标准       | 双层权限均被独立负例证明，保存回执不变                  |
| 明确不做       | 不用前端权限替代服务端，不提供管理员越权API             |

### V02-014

| 字段           | 要求                                                                   |
| -------------- | ---------------------------------------------------------------------- |
| ID             | V02-014                                                                |
| 任务           | 固定入口迁移至动态编辑路由，feat提交                                   |
| 前置依赖       | V02-003、004、005、011、013                                            |
| 代码影响范围   | web/app/resumes/[id]/edit、editor-entry、server/http                   |
| 数据库影响     | 无DDL                                                                  |
| API/页面影响   | `/`跳转、动态编辑页；真实模式禁bootstrap                               |
| 安全不变量     | Session+ID授权先于草稿恢复；禁止共享私有缓存                           |
| 必须新增的测试 | 两份本人文档切换、直接URL/刷新、401/404/损坏、RSC/后退/预取无A/B串数据 |
| 验证命令       | F、I、B、G                                                             |
| 完成证据       | 动态路由E2E、production构建响应头和缓存断言                            |
| 回滚方式       | 回前一真实Auth动态入口；不得公开回固定入口                             |
| 完成标准       | 任意本人授权ID正确装配原保存系统                                       |
| 明确不做       | 不重写Store/协调器/保存协议，不扩历史浏览                              |

### V02-015

| 字段           | 要求                                                      |
| -------------- | --------------------------------------------------------- |
| ID             | V02-015                                                   |
| 任务           | Session过期后保全及同账户恢复，fix/feat提交               |
| 前置依赖       | V02-014                                                   |
| 代码影响范围   | web/client/editor/persistence、saving、entry、tests       |
| 数据库影响     | 无DDL                                                     |
| API/页面影响   | 登录失效提示、session复核、原保存接口                     |
| 安全不变量     | pending原封不动，旧ACK不覆盖新输入                        |
| 必须新增的测试 | 过期时离线/IDB失败/刷新、同A重登重放、远端revision变化412 |
| 验证命令       | F、I、B、S                                                |
| 完成证据       | 原生Chrome IDB序号/回执断言                               |
| 回滚方式       | 暂停自动恢复，仅救援导出，保留原分区                      |
| 完成标准       | 认证未确认不上传；同账号可恢复且无假saved                 |
| 明确不做       | 不改本地草稿为另一账号，不新增离线登录                    |

### V02-016

| 字段           | 要求                                                                 |
| -------------- | -------------------------------------------------------------------- |
| ID             | V02-016                                                              |
| 任务           | 退出/换账户上下文屏障，feat/fix提交                                  |
| 前置依赖       | V02-015                                                              |
| 代码影响范围   | web/client账户动作、editor生命周期、persistence、tests/browser       |
| 数据库影响     | 撤Session，无DDL                                                     |
| API/页面影响   | 退出确认、救援/放弃、登录切换                                        |
| 安全不变量     | A正文绝不上传到B或显示于B；全账户未同步扫描                          |
| 必须新增的测试 | 同context A→B、多tab、迟到ACK、广播遗漏、离线退出、救援取消、bfcache |
| 验证命令       | F、I、B                                                              |
| 完成证据       | A/B服务端hash/版本无污染；本地分区清理报告                           |
| 回滚方式       | 禁换号自动恢复，保留救援；会话撤销不回滚                             |
| 完成标准       | 第12节每个事件有自动化断言，未确认清理零发生                         |
| 明确不做       | 不存Token在JS，不做远端擦盘承诺                                      |

### V02-017

| 字段           | 要求                                                       |
| -------------- | ---------------------------------------------------------- |
| ID             | V02-017                                                    |
| 任务           | 新建简历事务，feat提交                                     |
| 前置依赖       | V02-013                                                    |
| 代码影响范围   | domain/default、app/resume、infra/resume、web/api/resumes  |
| 数据库影响     | 复用resume/version/回执/审计；不新增模板                   |
| API/页面影响   | POST resumes，空白/基础样式                                |
| 安全不变量     | owner受信、UUID新建、初始revision=1原子提交                |
| 必须新增的测试 | 两模式空内容、重复key/并发/故障、拒绝owner、无原型个人资料 |
| 验证命令       | F、I、G                                                    |
| 完成证据       | 一次创建四类记录一致、Schema通过                           |
| 回滚方式       | 关闭创建，已建内容仍能编辑；回兼容应用                     |
| 完成标准       | 原key重放只得到同一新简历，无孤立版本                      |
| 明确不做       | 不导入文件，不多模板市场                                   |

### V02-018

| 字段           | 要求                                                          |
| -------------- | ------------------------------------------------------------- |
| ID             | V02-018                                                       |
| 任务           | 本人工作台元数据分页查询，feat提交                            |
| 前置依赖       | V02-013                                                       |
| 代码影响范围   | app/resume、infra/resume、web/api/resumes、tests              |
| 数据库影响     | 必要owner/updatedAt/id索引                                    |
| API/页面影响   | GET resumes及分页DTO                                          |
| 安全不变量     | 不带正文，无公共缓存，游标不跨账户                            |
| 必须新增的测试 | 空/20/21/100份、同时间稳定排序、非法/他人游标、已删过滤、故障 |
| 验证命令       | F、I、G                                                       |
| 完成证据       | DTO字段白名单/分页集合与EXPLAIN合成报告                       |
| 回滚方式       | 撤查询代码，索引可保留兼容                                    |
| 完成标准       | 同一稳定数据集翻页无漏重、其他账户零条                        |
| 明确不做       | 不搜索、不全文索引、不正文缩略图                              |

### V02-019

| 字段           | 要求                                                          |
| -------------- | ------------------------------------------------------------- |
| ID             | V02-019                                                       |
| 任务           | 重命名CAS闭环，feat提交                                       |
| 前置依赖       | V02-014                                                       |
| 代码影响范围   | app/infra resume、web/api及编辑器标题控件、tests              |
| 数据库影响     | 版本reason扩展（若需要）；同事务title快照                     |
| API/页面影响   | PATCH resume、编辑器标题输入                                  |
| 安全不变量     | 与保存共用revision；正文不被旧标题响应覆盖                    |
| 必须新增的测试 | 标题0/1/200/201含emoji、并发save/rename、丢ACK同key、失败回滚 |
| 验证命令       | F、I、B、G                                                    |
| 完成证据       | revision/title_snapshot/正文hash与UI回读                      |
| 回滚方式       | 关重命名入口，保留新revision兼容读取                          |
| 完成标准       | 重命名一次一个新版本，冲突明确412                             |
| 明确不做       | 不新增独立标题保存协议                                        |

### V02-020

| 字段           | 要求                                                                                  |
| -------------- | ------------------------------------------------------------------------------------- |
| ID             | V02-020                                                                               |
| 任务           | 当前快照复制为独立简历，feat提交                                                      |
| 前置依赖       | V02-017                                                                               |
| 代码影响范围   | domain命令/复制纯函数、app/infra resume、web/api/copies                               |
| 数据库影响     | 新resume与单initial版本/回执/审计                                                     |
| API/页面影响   | POST copies                                                                           |
| 安全不变量     | 当前owner固定，来源revision一致，不复制历史/草稿                                      |
| 必须新增的测试 | 全kind/link/block ID不相交，order/overrides引用有效，来源变动、跨账户、重放、失败回滚 |
| 验证命令       | F、I、G                                                                               |
| 完成证据       | UUID集合与Schema/hash投影断言                                                         |
| 回滚方式       | 关复制入口，不删除已成功副本                                                          |
| 完成标准       | 正文与样式等价、全部内容ID新生、历史仅1条                                             |
| 明确不做       | 不跨账户分享复制、不复制旧版本链                                                      |

### V02-021

| 字段           | 要求                                                       |
| -------------- | ---------------------------------------------------------- |
| ID             | V02-021                                                    |
| 任务           | 可恢复软删除生命周期，feat提交                             |
| 前置依赖       | V02-019；第23节删除政策批准                                |
| 代码影响范围   | app/infra resume、database迁移、web/api删除/trash/undelete |
| 数据库影响     | restore_until、deletion_events、版本reason、RLS/索引       |
| API/页面影响   | DELETE/trash/undelete合同                                  |
| 安全不变量     | 普通路径已删404；删除/恢复都CAS，不直接回退revision        |
| 必须新增的测试 | 期限±1秒、并发保存/删除/恢复、他人404、重放、故障全回滚    |
| 验证命令       | F、I、M、S                                                 |
| 完成证据       | 生命周期/事件/版本断言                                     |
| 回滚方式       | 停删除/恢复入口；保留墓碑和期限，不批量undelete            |
| 完成标准       | 状态/版本/事件同事务，越权零成功                           |
| 明确不做       | 不建Job队列，不永久删除按钮                                |

### V02-022

| 字段           | 要求                                                               |
| -------------- | ------------------------------------------------------------------ |
| ID             | V02-022                                                            |
| 任务           | 最小工作台与创建页闭环，feat提交                                   |
| 前置依赖       | V02-016、017、018、019、020、021                                   |
| 代码影响范围   | web/app/resumes、client/dashboard、tests/unit/browser              |
| 数据库影响     | 无DDL                                                              |
| API/页面影响   | 列表/创建/卡片动作/最近编辑/账户退出                               |
| 安全不变量     | 卡片无正文泄露；错误不丢输入，不盲目重建                           |
| 必须新增的测试 | 空/加载/失败/列表/分页、新建进入、重命名/复制/删除确认及失败保卡片 |
| 验证命令       | F、B、G                                                            |
| 完成证据       | 原型对照截图和工作台任务录像的脱敏摘要                             |
| 回滚方式       | 回前一兼容UI，资源持久化不撤回                                     |
| 完成标准       | 普通用户不用API工具走完所有卡片动作                                |
| 明确不做       | 不搜索、不额度会员AI/PDF按钮、不管理后台                           |

### V02-023

| 字段           | 要求                                                               |
| -------------- | ------------------------------------------------------------------ |
| ID             | V02-023                                                            |
| 任务           | 最近删除/恢复与草稿冲突反馈，feat提交                              |
| 前置依赖       | V02-021、V02-022                                                   |
| 代码影响范围   | web/app/resumes/trash、client/editor资源失效分支、tests            |
| 数据库影响     | 无DDL                                                              |
| API/页面影响   | trash列表、恢复按钮、其他tab删除提示                               |
| 安全不变量     | 已删正文不预览；未同步内容先救援不自动恢复                         |
| 必须新增的测试 | 空/期限到期/恢复失败、另tab旧pending保存、恢复后基线冲突与键盘焦点 |
| 验证命令       | F、I、B、G                                                         |
| 完成证据       | trash/编辑器状态截图与无自动上传断言                               |
| 回滚方式       | 停恢复UI，保留墓碑，按政策人工受控恢复                             |
| 完成标准       | 删除含义和期限明确，恢复不触发旧pending写入                        |
| 明确不做       | 不扩完整历史中心                                                   |

### V02-024

| 字段           | 要求                                                                                                 |
| -------------- | ---------------------------------------------------------------------------------------------------- |
| ID             | V02-024                                                                                              |
| 任务           | 受控有界硬删除清理，feat提交                                                                         |
| 前置依赖       | V02-021                                                                                              |
| 代码影响范围   | infra/privacy、platform/scripts/ops、database授权/回执关联迁移                                       |
| 数据库影响     | 清理角色、显式回执资源关联、版本/正文清理                                                            |
| API/页面影响   | 无用户永久删除端点                                                                                   |
| 安全不变量     | 仅过期目标、小批锁定、不可由普通Web调用；删除事件独立留存确认                                        |
| 必须新增的测试 | dry-run无写、非测试目标拒绝、到期边界、恢复竞争、崩溃重入、自引用版本/回执完整清理                   |
| 验证命令       | F、I、M；CLI安全负例通过现有tests目录执行                                                            |
| 完成证据       | 合成目标清理计数/遗漏为0，无正文日志                                                                 |
| 回滚方式       | 停定时触发；已合法硬删不恢复为可访问内容                                                             |
| 完成标准       | 24小时清理SLO可监控，重跑幂等，无超范围删除；独立删除清单通道未由026验收前，真实环境禁止启用清理调度 |
| 明确不做       | 不构建Worker/通用调度器/未来派生表                                                                   |

### V02-025

| 字段           | 要求                                                                         |
| -------------- | ---------------------------------------------------------------------------- |
| ID             | V02-025                                                                      |
| 任务           | 账户注销请求到清理闭环，feat提交                                             |
| 前置依赖       | V02-010、V02-016、V02-024                                                    |
| 代码影响范围   | app/infra privacy、web/api/account和account页面、tests                       |
| 数据库影响     | privacy_requests及RLS；停用、撤Auth/清所有owner数据                          |
| API/页面影响   | DELETE account、重新认证/救援/确认与受理文案                                 |
| 安全不变量     | 先禁止业务再清理；不能以邮箱冒充本人；重新注册新UUID                         |
| 必须新增的测试 | 过期重认证、重复请求、Auth撤销暂失败、写入/注销竞争、清理重入、旧Session无效 |
| 验证命令       | F、I、M、B、G                                                                |
| 完成证据       | 注销时间线、所有相关对象剩余计数、用户状态回读                               |
| 回滚方式       | 停新受理仍履行已受理请求，不恢复已注销权限                                   |
| 完成标准       | 立即业务禁用、24小时清理、无孤立凭据/正文                                    |
| 明确不做       | 不做账号合并、注销反悔/会员退款                                              |

### V02-026

| 字段           | 要求                                                                   |
| -------------- | ---------------------------------------------------------------------- |
| ID             | V02-026                                                                |
| 任务           | 备份恢复后的删除防复活，feat/test提交                                  |
| 前置依赖       | V02-024、V02-025                                                       |
| 代码影响范围   | platform/scripts/ops恢复与独立清单导出、tests/integration              |
| 数据库影响     | 恢复库重放事件、撤旧Session/Token，无生产写入                          |
| API/页面影响   | 恢复后准入检查，不建用户页面                                           |
| 安全不变量     | 水位缺失fail-closed；尊重最后恢复事件和硬删终态                        |
| 必须新增的测试 | 备份后删除/undelete/注销、清单缺尾/重复/乱序、重放两次、恢复旧认证记录 |
| 验证命令       | F、I、R、N                                                             |
| 完成证据       | 清单hash/水位、恢复前后不可见断言、耗时                                |
| 回滚方式       | 保持恢复库隔离；不切流量                                               |
| 完成标准       | 正确资源删除/恢复均符合最新事件，未知水位绝不开放                      |
| 明确不做       | 不新增对象存储或假称逻辑dump提供PITR                                   |

### V02-027

| 字段           | 要求                                                                  |
| -------------- | --------------------------------------------------------------------- |
| ID             | V02-027                                                               |
| 任务           | V0.1升级及兼容回滚演练，test/docs提交                                 |
| 前置依赖       | V02-008、021、024、025、026                                           |
| 代码影响范围   | tests/integration/migrations、database回滚说明、scripts/ops           |
| 数据库影响     | 隔离旧库夹具升级；正式无破坏性down                                    |
| API/页面影响   | 新旧兼容应用读写验证                                                  |
| 安全不变量     | 旧hash/owner/revision不变，公开回滚不退fixed                          |
| 必须新增的测试 | 空库/0001/重跑/checksum损坏/中断、旧版本读、新版本写、回滚后CAS与角色 |
| 验证命令       | F、M、I、S、R、N、G                                                   |
| 完成证据       | 迁移版本、兼容矩阵、恢复和回滚实测报告                                |
| 回滚方式       | 演练库重建；生产仅兼容应用回滚/forward-fix                            |
| 完成标准       | 所有支持路径通过；不支持回退路径显式阻断                              |
| 明确不做       | 不复制真实生产简历到测试，不自动down生产                              |

### V02-028

| 字段           | 要求                                                            |
| -------------- | --------------------------------------------------------------- |
| ID             | V02-028                                                         |
| 任务           | 双真实账户Chrome端到端矩阵，test提交                            |
| 前置依赖       | V02-022、023、025、027                                          |
| 代码影响范围   | tests/browser、Playwright配置/安全Fake邮件测试装配              |
| 数据库影响     | 临时注册随机账号与合成简历、限定清理                            |
| API/页面影响   | 全部本期用户主线及异常                                          |
| 安全不变量     | 不用fixed冒充Auth；测试邮件读取能力生产不存在                   |
| 必须新增的测试 | T01—T12跨层主线、A/B context+同context换号、旧ACK/刷新/服务重启 |
| 验证命令       | F、I、B、G                                                      |
| 完成证据       | 每用例状态、浏览器/构建版本、脱敏网络断言                       |
| 回滚方式       | 不撤旧测试；修测试隔离问题，不降低断言                          |
| 完成标准       | Chrome原生IDB+真PG+真Auth成功，无skip/only                      |
| 明确不做       | 不以DOM mock或固定账号替代                                      |

### V02-029

| 字段           | 要求                                              |
| -------------- | ------------------------------------------------- |
| ID             | V02-029                                           |
| 任务           | 视觉与无障碍验收，test/style提交                  |
| 前置依赖       | V02-028                                           |
| 代码影响范围   | tests/browser和必要局部UI修复，platform/docs验收  |
| 数据库影响     | 无DDL                                             |
| API/页面影响   | 认证/工作台空列表错误/编辑/trash/account          |
| 安全不变量     | 视觉不能省错误/救援或藏不可达控件                 |
| 必须新增的测试 | T13/T14全尺寸、键盘焦点、200%、自动检查与NVDA人工 |
| 验证命令       | F、B、G；人工执行第15节流程并签名                 |
| 完成证据       | 基准截图差异原因、人工读屏记录                    |
| 回滚方式       | 局部revert；基线不自动批准失败差异                |
| 完成标准       | 阻断性无障碍/裁切/溢出为0，状态均可理解           |
| 明确不做       | 不借验收改品牌或大重构UI                          |

### V02-030

| 字段           | 要求                                                     |
| -------------- | -------------------------------------------------------- |
| ID             | V02-030                                                  |
| 任务           | 真实Session路径性能预算验收，test/fix提交                |
| 前置依赖       | V02-028                                                  |
| 代码影响范围   | tests/browser/integration、performance基准与必要根因修复 |
| 数据库影响     | 只按测量添加必要索引；迁移另审                           |
| API/页面影响   | 登录后列表/CRUD/编辑保存                                 |
| 安全不变量     | 不降哈希/RLS/CAS、不给性能测试绕Auth                     |
| 必须新增的测试 | 第16节样本数、边界文档、100份列表、连接池稳态与错误率    |
| 验证命令       | F、I、B、P、G                                            |
| 完成证据       | 硬件/网络/p50/p95/p99/失败数及优化前后对比               |
| 回滚方式       | 撤根因修复但保持门禁阻断，不放宽预算                     |
| 完成标准       | 原预算与新增批准预算全部真实测量通过                     |
| 明确不做       | 不加缓存框架/Redis、不虚构容量SLA                        |

### V02-031

| 字段           | 要求                                                                    |
| -------------- | ----------------------------------------------------------------------- |
| ID             | V02-031                                                                 |
| 任务           | 安全脱敏与运行告警交付，fix/chore提交                                   |
| 前置依赖       | V02-012、024、025                                                       |
| 代码影响范围   | web/server日志/头、scripts/ops、tests、platform/docs运行手册            |
| 数据库影响     | 审计清理策略/最小字段，必要迁移须M                                      |
| API/页面影响   | 安全头、错误响应，不加运营后台                                          |
| 安全不变量     | 密码/Token/正文/SQL参数不入证据，告警不携带敏感值                       |
| 必须新增的测试 | 合成秘密canary过滤、依赖客户端泄露负例、CSP、备份/邮件/清理失败告警注入 |
| 验证命令       | F、I、B、G；`pnpm audit --prod`（需联网，结果人工判定可利用高危阻断）   |
| 完成证据       | 脱敏安全报告、依赖审查、告警送达和运行手册                              |
| 回滚方式       | 停受影响入口/遥测，保留安全审计；不关闭必要防护                         |
| 完成标准       | canary无泄露，告警有实际接收人，已知可利用高危为0                       |
| 明确不做       | 不上线Replay/第三方分析，不搭新监控平台                                 |

### V02-032

| 字段           | 要求                                                                          |
| -------------- | ----------------------------------------------------------------------------- |
| ID             | V02-032                                                                       |
| 任务           | 全部V0.2门禁及脱敏证据汇总，test/chore提交                                    |
| 前置依赖       | V02-027、028、029、030、031                                                   |
| 代码影响范围   | .github/workflows/verify.yml、platform/scripts/ci、Playwright配置、tests/unit |
| 数据库影响     | 一次性Auth/业务角色与随机测试账号                                             |
| API/页面影响   | 无产品入口                                                                    |
| 安全不变量     | 无生产secret、固定/真实模式隔离，任一必需失败总体失败                         |
| 必须新增的测试 | 缺报告/失败子步骤/跳过/不完整Auth项目拒绝；浏览器失败仍执行原生恢复           |
| 验证命令       | F、ALL；用户手动push后读取对应SHA Actions                                     |
| 完成证据       | 完整矩阵、首次失败/重试、报告清单hash与保留期限                               |
| 回滚方式       | 回前CI仅作内部使用；不能绕门禁发布                                            |
| 完成标准       | 缺证据失败，全部门禁有同SHA可见结果                                           |
| 明确不做       | 不自动push，不生产自动部署，不上传原始Auth trace                              |

### V02-033

| 字段           | 要求                                                                            |
| -------------- | ------------------------------------------------------------------------------- |
| ID             | V02-033                                                                         |
| 任务           | staging真实依赖与发布/恢复演练，chore/docs提交                                  |
| 前置依赖       | V02-032；第18节环境/邮件/域名与凭据获授权                                       |
| 代码影响范围   | platform/docs运行手册、必要deploy配置，不改业务范围                             |
| 数据库影响     | 独立staging迁移/PITR恢复与删除重放，不写生产                                    |
| API/页面影响   | HTTPS真实Session/邮件全流程                                                     |
| 安全不变量     | 合成数据、批准目标、同digest；无法安全回退不强退fixed                           |
| 必须新增的测试 | 真实收件/退信、HTTPS Cookie、受控代理、多进程限流、回滚/轮换/告警/PITR与RPO/RTO |
| 验证命令       | F、ALL；云平台操作在本任务交付经审查的实际命令清单，配置未定前不执行            |
| 完成证据       | staging版本、域/区域、投递时间、实际回滚与RPO/RTO、操作者审批                   |
| 回滚方式       | 切回兼容Auth构建；恢复失败保持隔离，不开用户流量                                |
| 完成标准       | 第18节技术外部项全部有真实证据，不以Fake替代                                    |
| 明确不做       | 不自动购买资源/选邮件供应商、不发布生产                                         |

### V02-034

| 字段           | 要求                                         |
| -------------- | -------------------------------------------- |
| ID             | V02-034                                      |
| 任务           | 签署A代码候选版验收，docs提交                |
| 前置依赖       | V02-032，所含所有代码任务完成                |
| 代码影响范围   | 本计划状态/README候选版说明/验收记录         |
| 数据库影响     | 无新改动                                     |
| API/页面影响   | 记录交付范围，不开公网                       |
| 安全不变量     | 不能把A称B；不能套用旧SHA绿色                |
| 必须新增的测试 | 证据清单完整性、未关闭P0/P1/skip/外部项检查  |
| 验证命令       | D、F、ALL；核对用户手动push后的当前SHA运行   |
| 完成证据       | 第26节A勾选表、审阅者、CI和本地报告          |
| 回滚方式       | 撤销候选版标记，不回退用户内容               |
| 完成标准       | 本地+CI全部通过，范围内无P0/P1，外部缺口明确 |
| 明确不做       | 不声称中国大陆可公开上线                     |

### V02-035

| 字段           | 要求                                                       |
| -------------- | ---------------------------------------------------------- |
| ID             | V02-035                                                    |
| 任务           | 签署B中国大陆公开发布就绪决策，docs提交                    |
| 前置依赖       | V02-033、V02-034；所有外部阻断关闭                         |
| 代码影响范围   | 发布清单/运行手册/政策链接，无新业务代码                   |
| 数据库影响     | 核验生产角色/备份配置，实际发布另需明确授权                |
| API/页面影响   | 批准域名/隐私协议/支持入口已就绪                           |
| 安全不变量     | 不缺备案适用性/邮件/删除/PITR/值班证据即放行               |
| 必须新增的测试 | B准入逐项证据匹配digest/日期；缺任一阻断                   |
| 验证命令       | D、ALL（隔离验收）；人工逐项复核18/26节，不用假release脚本 |
| 完成证据       | 主体/运营条件/政策/技术/安全签署与发布范围                 |
| 回滚方式       | 撤销就绪批准并保持不公开；事故按手册撤会话/隔离            |
| 完成标准       | B全部满足，无P0/P1，用户明确批准上线动作                   |
| 明确不做       | 不自动部署生产，不因签署而自动进入AI开发                   |

## 26. V0.2 完成定义

### A：代码候选版

- 用户批准范围；M0遗留问题有当前SHA证据；无模拟未来入口。
- 真Auth/PG Session与验证/重置/退出完整；无Token进入JS、日志/公开产物。
- 两个真实账户的API/PG RLS/Chrome越权负例全部通过。
- 动态编辑、多简历CRUD、复制ID、删除/恢复、注销、IDB过期/换号/救援全部通过。
- 保留既有Schema/CAS/幂等/快照/恢复测试；原性能预算不降。
- 新旧库迁移、逻辑和原生恢复、删除清单重放、兼容应用回滚全部通过。
- 本地与对应SHA CI必需矩阵通过，人工视觉/键盘/读屏通过；无已知P0/P1，无缺失/跳过必需证据。
- 清楚标注尚未完成的staging邮件/生产地域/合规/PITR等B项，称“V0.2代码候选版”。

### B：中国大陆公开发布就绪

A成立，加第18节所有外部阻断关闭、第33任务同digest staging成功、真实邮件送达、生产密钥/轮换、备份加密/PITR/RPO/RTO、删除政策和恢复防复活、监控告警/运行手册、主体/域名/适用运营条件及政策批准、明确发布责任人。尚缺任何一项，只能保持A，不以“功能能用”替代B。

本文件批准不等于执行生产部署；实际发布需另行明确目标、变更单和授权。未获得审阅批准前，V0.2业务编码不开始。

## 27. V0.3 AI 实验进入条件

V0.2拟参与实验的环境达到相应A/B门槛；身份隔离、草稿/版本保全、删除与恢复经真实使用验证；无未解决P0/P1；用户另行批准单项实验目标、参与范围、成功/停止标准、数据用途/供应商数据路径/成本上限。公开实验还须完成相应AI适用合规评估。

本稿不选择AI Provider、不设计Prompt/Job/额度/建议模型、不建表、不实现接口。注册赠额、PDF/分享仍不能作为V0.3默认依赖；是否进入下一阶段由单独评审决定。

## 附录：本轮交付与下一动作

本轮只新增本文件；没有改业务代码、既有文档、数据库或依赖。文档结构/链接/依赖检查不等于F/I/B/N重新通过。既有未推送代码提交444d646仍原样保留；本稿如本地提交仅使用docs类型，不自动推送。

草案验收要求：27个编号章节、35个不重复任务、每任务13个必需字段；索引中所有任务依赖存在且无环；Markdown表格列数一致；格式检查通过；已有技术文档结构校验保持通过。文档检查失败先修本文，不能为了草案改写旧业务代码或迁移。

审阅顺序建议：先第5/23节范围和政策，再第9/12/13/20节身份/草稿/删除/恢复，最后第25/26节任务与门禁。批准后从001及独立M0稳定性任务开始，不能跳到AI、PDF或收费。
